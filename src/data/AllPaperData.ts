import { CAA20 } from './CAA20';
import { CGK21 } from './CGK21';
import { CSL12 } from './CSL12';
import { DMC09 } from './DMC09';
import { EAIA17 } from './EAIA17';
import { EYG15 } from './EYG15';
import { FCF09 } from './FCF09';
import { GGS12MT } from './GGS12MT';
import { GGS12ST } from './GGS12ST';
import { GGS12TPS } from './GGS12TPS';
import { HYZ13 } from './HYZ13';
import { JTM11 } from './JTM11';
import { JSM16 } from './JSM16';
import { JX17 } from './JX17';
import { KSBK15 } from './KSBK15';
import { LGX16 } from './LGX16';
import { LJC18 } from './LJC18';
import { LLN14 } from './LLN14';
import { LWW13 } from './LWW13';
import { MA16 } from './MA16';
import { MDD10 } from './MDD10';
import { MEV16 } from './MEV16';
import { MHK16 } from './MHK16';
import { MVM17 } from './MVM17';
import { MWSO08 } from './MWSO08';
import { PC15PV } from './PC15PV';
import { REP15 } from './REP15';
import { SCS16 } from './SCS16';
import { SCS17 } from './SCS17';
import { SCS19 } from './SCS19';
import { SSK12 } from './SSK12';
import { VWH13 } from './VWH13';
import { XD99 } from './XD99';
import { XWL17 } from './XWL17';
import { XWS16 } from './XWS16';
import { YFS10 } from './YFS10';
import { ZGWZ14 } from './ZGWZ14';
import { ZLW13 } from './ZLW13';
import { SRJ13 } from './SRJ13';
import { RSRY12 } from './RSRY12';
import { FAKM15 } from './FAKM15';
import { YSK14 } from './YSK14';
import { CSL16 } from './CSL16';
import { RRR19 } from './RRR19';
import { YCC20 } from './YCC20';
import { MDDD16 } from './MDDD16';
import { GYS14 } from './GYS14';
import { MDJW07 } from './MDJW07';
import { KWL09 } from './KWL09';
import { BN11B } from './BN11B';
import { MLG06 } from './MLG06';
import { FA20 } from './FA20';
import { WHWS12 } from './WHWS12';
import { FZC18 } from './FZC18';
import { ATSC06 } from './ATSC06';
import { DJM12 } from './DJM12';
import { MEA17 } from './MEA17';
import { SFJ16 } from './SFJ16';
import { XJX21 } from './XJX21';
import { ASG21 } from './ASG21';
import { SankeyLink, SankeyLinkExtended } from '../types';
import { ClusterOne } from './ClusterOne';
import { ClusterTwo } from './ClusterTwo';
import { ClusterThree } from './ClusterThree';
import { ClusterFour } from './ClusterFour';
import { ClusterFourr } from './ClusterFours';
import { Int1 } from './netIntNode/Int1';
import { Int2 } from './netIntNode/Int2';
import { Int3 } from './netIntNode/Int3';
import { Int4 } from './netIntNode/Int4';
import { Int5 } from './netIntNode/Int5';
import { Int6 } from './netIntNode/Int6';
import { Int7 } from './netIntNode/Int7';
import { Int8 } from './netIntNode/Int8';
import { Int9 } from './netIntNode/Int9';
import { Int10 } from './netIntNode/Int10';
import { Int11 } from './netIntNode/Int11';
import { Int12 } from './netIntNode/Int12';
import { Int13 } from './netIntNode/Int13';
import { Int14 } from './netIntNode/Int14';
import { Int15 } from './netIntNode/Int15';
import { Int16 } from './netIntNode/Int16';
import { Rep1 } from './netrepNode/Rep1';
import { Rep2 } from './netrepNode/Rep2';
import { Rep3 } from './netrepNode/Rep3';
import { Rep4 } from './netrepNode/Rep4';
import { Rep5 } from './netrepNode/Rep5';
import { Rep6 } from './netrepNode/Rep6';
import { Rep7 } from './netrepNode/Rep7';
import { Rep8 } from './netrepNode/Rep8';
import { Rep9 } from './netrepNode/Rep9';
import { Rep10 } from './netrepNode/Rep10';
import { Rep11 } from './netrepNode/Rep11';
import { Rep12 } from './netrepNode/Rep12';
import { Rep13 } from './netrepNode/Rep13';
import { Rep14 } from './netrepNode/Rep14';
import { Rep15 } from './netrepNode/Rep15';
import { Rep16 } from './netrepNode/Rep16';
import { Rep17 } from './netrepNode/Rep17';
import { Rep18 } from './netrepNode/Rep18';
import { Rep19 } from './netrepNode/Rep19';
import { Rep20 } from './netrepNode/Rep20';
import { Rep21 } from './netrepNode/Rep21';
import { Rep22 } from './netrepNode/Rep22';
import { Rep23 } from './netrepNode/Rep23';
import { Rep24 } from './netrepNode/Rep24';
import { Tec1 } from './nettecNode/Tec1';
import { Tec2 } from './nettecNode/Tec2';
import { Tec3 } from './nettecNode/Tec3';
import { Tec4 } from './nettecNode/Tec4';
import { Tec5 } from './nettecNode/Tec5';
import { Tec6 } from './nettecNode/Tec6';
import { Tec7 } from './nettecNode/Tec7';
import { Tec8 } from './nettecNode/Tec8';
import { Tec9 } from './nettecNode/Tec9';
import { Tec10 } from './nettecNode/Tec10';
import { Tec11 } from './nettecNode/Tec11';
import { Tec12 } from './nettecNode/Tec12';
import { Tec13 } from './nettecNode/Tec13';
import { Tec14 } from './nettecNode/Tec14';
import { Tec15 } from './nettecNode/Tec15';
import { Var1 } from './netvarNode/Var1';
import { Var2 } from './netvarNode/Var2';
import { Var3 } from './netvarNode/Var3';
import { Var4 } from './netvarNode/Var4';
import { Var5 } from './netvarNode/Var5';
import { Var6 } from './netvarNode/Var6';
import { Var7 } from './netvarNode/Var7';
import { Tar1 } from './nettarNode/Tar1';
import { Tar2 } from './nettarNode/Tar2';
import { Tar3 } from './nettarNode/Tar3';
import { Tar4 } from './nettarNode/Tar4';
import { Tar5 } from './nettarNode/Tar5';
import { Tar6 } from './nettarNode/Tar6';
import { Tar7 } from './nettarNode/Tar7';
import { Tar8 } from './nettarNode/Tar8';
import { Tar9 } from './nettarNode/Tar9';
import { Tar10 } from './nettarNode/Tar10';
import { Tar11 } from './nettarNode/Tar11';
import { Tar12 } from './nettarNode/Tar12';
import { Tar13 } from './nettarNode/Tar13';
import { Tar14 } from './nettarNode/Tar14';
import { Tar15 } from './nettarNode/Tar15';
import { Tar16 } from './nettarNode/Tar16';
import { Tar17 } from './nettarNode/Tar17';
import { Tar18 } from './nettarNode/Tar18';
import { Tar19 } from './nettarNode/Tar19';
import { Tar20 } from './nettarNode/Tar20';
import { Tar21 } from './nettarNode/Tar21';
import { Tar22 } from './nettarNode/Tar22';
import { Tar23 } from './nettarNode/Tar23';
import { Tar24 } from './nettarNode/Tar24';
import { Tar25 } from './nettarNode/Tar25';
import { Tar26 } from './nettarNode/Tar26';
import { Tar27 } from './nettarNode/Tar27';

const ClusteroneLinkData = [ClusterOne];
const ClustertwoLinkData = [ClusterTwo];
const ClusterthreeLinkData = [ClusterThree];
const ClusterfourLinkData = [ClusterFour, ClusterFourr];
const Int1Data = [Int1];
const Int2Data = [Int2];
const Int3Data = [Int3];
const Int4Data = [Int4];
const Int5Data = [Int5];
const Int6Data = [Int6];
const Int7Data = [Int7];
const Int8Data = [Int8];
const Int9Data = [Int9];
const Int10Data = [Int10];
const Int11Data = [Int11];
const Int12Data = [Int12];
const Int13Data = [Int13];
const Int14Data = [Int14];
const Int15Data = [Int15];
const Int16Data = [Int16];
const Rep1Data = [Rep1];
const Rep2Data = [Rep2];
const Rep3Data = [Rep3];
const Rep4Data = [Rep4];
const Rep5Data = [Rep5];
const Rep6Data = [Rep6];
const Rep7Data = [Rep7];
const Rep8Data = [Rep8];
const Rep9Data = [Rep9];
const Rep10Data = [Rep10];
const Rep11Data = [Rep11];
const Rep12Data = [Rep12];
const Rep13Data = [Rep13];
const Rep14Data = [Rep14];
const Rep15Data = [Rep15];
const Rep16Data = [Rep16];
const Rep17Data = [Rep17];
const Rep18Data = [Rep18];
const Rep19Data = [Rep19];
const Rep20Data = [Rep20];
const Rep21Data = [Rep21];
const Rep22Data = [Rep22];
const Rep23Data = [Rep23];
const Rep24Data = [Rep24];
const Var1Data = [Var1];
const Var2Data = [Var2];
const Var3Data = [Var3];
const Var4Data = [Var4];
const Var5Data = [Var5];
const Var6Data = [Var6];
const Var7Data = [Var7];
const Tec1Data = [Tec1];
const Tec2Data = [Tec2];
const Tec3Data = [Tec3];
const Tec4Data = [Tec4];
const Tec5Data = [Tec5];
const Tec6Data = [Tec6];
const Tec7Data = [Tec7];
const Tec8Data = [Tec8];
const Tec9Data = [Tec9];
const Tec10Data = [Tec10];
const Tec11Data = [Tec11];
const Tec12Data = [Tec12];
const Tec13Data = [Tec13];
const Tec14Data = [Tec14];
const Tec15Data = [Tec15];
const Tar1Data = [Tar1];
const Tar2Data = [Tar2];
const Tar3Data = [Tar3];
const Tar4Data = [Tar4];
const Tar5Data = [Tar5];
const Tar6Data = [Tar6];
const Tar7Data = [Tar7];
const Tar8Data = [Tar8];
const Tar9Data = [Tar9];
const Tar10Data = [Tar10];
const Tar11Data = [Tar11];
const Tar12Data = [Tar12];
const Tar13Data = [Tar13];
const Tar14Data = [Tar14];
const Tar15Data = [Tar15];
const Tar16Data = [Tar16];
const Tar17Data = [Tar17];
const Tar18Data = [Tar18];
const Tar19Data = [Tar19];
const Tar20Data = [Tar20];
const Tar21Data = [Tar21];
const Tar22Data = [Tar22];
const Tar23Data = [Tar23];
const Tar24Data = [Tar24];
const Tar25Data = [Tar25];
const Tar26Data = [Tar26];
const Tar27Data = [Tar27];

//@ts-ignore
const a = [];
ClusterTwo.filter((want) => {
    if (want.target) {
        a.push(want.id);
    }
});
//@ts-ignore
// console.log(a);

//@ts-ignore
const Empty = [];
//@ts-ignore
const PaperString = [
    CAA20.links,
    CGK21.links,
    CSL12.links,
    DMC09.links,
    EAIA17.links,
    EYG15.links,
    FCF09.links,
    GGS12MT.links,
    GGS12ST.links,
    GGS12TPS.links,
    HYZ13.links,
    JTM11.links,
    JSM16.links,
    JX17.links,
    KSBK15.links,
    LGX16.links,
    LJC18.links,
    LLN14.links,
    LWW13.links,
    MA16.links,
    MDD10.links,
    MEV16.links,
    MHK16.links,
    MVM17.links,
    MWSO08.links,
    PC15PV.links,
    REP15.links,
    SCS16.links,
    SCS17.links,
    SCS19.links,
    SSK12.links,
    VWH13.links,
    XD99.links,
    XWL17.links,
    XWS16.links,
    YFS10.links,
    ZGWZ14.links,
    ZLW13.links,
    SRJ13.links,
    RSRY12.links,
    FAKM15.links,
    YSK14.links,
    CSL16.links,
    RRR19.links,
    YCC20.links,
    MDDD16.links,
    GYS14.links,
    MDJW07.links,
    KWL09.links,
    BN11B.links,
    MLG06.links,
    FA20.links,
    WHWS12.links,
    FZC18.links,
    ATSC06.links,
    DJM12.links,
    MEA17.links,
    SFJ16.links,
    XJX21.links,
    ASG21.links,
];

// console.log(PaperString.length);

const Papers = [
    BN11B,
    CAA20,
    CGK21,
    CSL12,
    CSL16,
    DMC09,
    EAIA17,
    EYG15,
    FA20,
    FAKM15,
    FCF09,
    GGS12MT,
    GGS12ST,
    GGS12TPS,
    GYS14,
    HYZ13,
    JTM11,
    JSM16,
    JX17,
    KSBK15,
    KWL09,
    LGX16,
    LJC18,
    LLN14,
    LWW13,
    MA16,
    MDD10,
    MDDD16,
    MDJW07,
    MEV16,
    MHK16,
    MLG06,
    MVM17,
    MWSO08,
    PC15PV,
    REP15,
    RRR19,
    RSRY12,
    SCS16,
    SCS17,
    SCS19,
    SRJ13,
    SSK12,
    VWH13,
    XD99,
    XWL17,
    XWS16,
    YCC20,
    YFS10,
    YSK14,
    ZGWZ14,
    ZLW13,
    WHWS12,
    FZC18,
    ATSC06,
    DJM12,
    MEA17,
    SFJ16,
    XJX21,
    ASG21,
];

//@ts-ignore
const PaperStatus = [
    CAA20.status,
    CGK21.status,
    CSL12.status,
    DMC09.status,
    EAIA17.status,
    EYG15.status,
    FCF09.status,
    GGS12MT.status,
    GGS12ST.status,
    GGS12TPS.status,
    HYZ13.status,
    JTM11.status,
    JSM16.status,
    JX17.status,
    KSBK15.status,
    LGX16.status,
    LJC18.status,
    LLN14.status,
    LWW13.status,
    MA16.status,
    MDD10.status,
    MEV16.status,
    MHK16.status,
    MVM17.status,
    MWSO08.status,
    PC15PV.status,
    REP15.status,
    SCS16.status,
    SCS17.status,
    SCS19.status,
    SSK12.status,
    VWH13.status,
    XD99.status,
    XWL17.status,
    XWS16.status,
    YFS10.status,
    ZGWZ14.status,
    ZLW13.status,
    SRJ13.status,
    RSRY12.status,
    FAKM15.status,
    YSK14.status,
    CSL16.status,
    RRR19.status,
    YCC20.status,
    MDDD16.status,
    GYS14.status,
    MDJW07.status,
    KWL09.status,
    BN11B.status,
    MLG06.status,
    FA20.status,
    WHWS12.status,
    FZC18.status,
    ATSC06.status,
    DJM12.status,
    MEA17.status,
    SFJ16.status,
    XJX21.status,
    ASG21.status,
];

//TODO 유형 추가시 계속 변수 생성하기.
// console.log(PaperString);
//@ts-ignore
const test = () => {
    //@ts-ignore
    const paper = PaperStatus.map((data) => data);
    return paper;
};
const Status = test();

const check = () => {
    const a = PaperString;
    // console.log(a.reduce((acc, curr) => (acc += curr.length), 0));
    return a;
};

check();

//@ts-ignore
const Emptys = [].concat.apply([], Empty).reduce((result, value) => {
    //@ts-ignore
    const target = result.find((r) => r.source === value.source && r.target === value.target);
    //@ts-ignore
    if (!target) result.push({ source: value.source, target: value.target, value: value.value });
    //@ts-ignore
    else target.value += 1;
    return result;
}, []);

//@ts-ignore
const AllPaperDatas = ([].concat.apply([], PaperString) as SankeyLink[]).reduce<SankeyLinkExtended[]>((AllPaperDatas, onePaper) => {
    //@ts-ignore
    const allOnePaper = AllPaperDatas.find(
        (r) =>
            r.source === onePaper.source &&
            r.target === onePaper.target &&
            r.value === onePaper.value &&
            r.valueid === onePaper.valueid &&
            r.process === onePaper.process &&
            r.paperName === onePaper.paperName
    );
    if (!allOnePaper)
        //@ts-ignore
        AllPaperDatas.push({
            source: onePaper.source,
            target: onePaper.target,
            value: onePaper.value,
            valueid: onePaper.valueid,
            paperName: onePaper.paperName,
            process: onePaper.process,
            id: onePaper.id,
            imgUrl: onePaper.imgUrl,
        });
    //@ts-ignore
    else allOnePaper.value += 1;
    return AllPaperDatas;
}, []);

// console.log(AllPaperDatas);
//@ts-ignore
const wantLink = [];
const trashLink = [];
AllPaperDatas.filter((want) => {
    if (want.source >= 50 && want.source <= 75 && want.target >= 83) {
        wantLink.push(want);
    } else trashLink.push(want);
});
//@ts-ignore
console.log(wantLink);

//@ts-ignore
const TargetAAs = ([].concat.apply([], PaperString) as SankeyLink[]).reduce<SankeyLinkExtended[]>((TargetAAs, onePaper) => {
    //@ts-ignore
    const targetaaOnePaper = TargetAAs.find(
        (r) => r.source === onePaper.source && r.target === onePaper.target && r.value === onePaper.value && r.valueid === onePaper.valueid && r.process === onePaper.process
    );

    if (!targetaaOnePaper) {
        //@ts-ignore
        TargetAAs.push({
            source: onePaper.source,
            target: onePaper.target,
            value: onePaper.value,
            valueid: onePaper.valueid,
            paperName: onePaper.paperName,
            process: onePaper.process,
            imgUrl: onePaper.imgUrl,

            id: onePaper.id, //TODO 추출시 필요한 변수만 넣기
        });
    } else {
        targetaaOnePaper.value += 1;
    }
    return TargetAAs;
}, []);

// console.log(TargetAAs);

//@ts-ignore
const TargetABs = ([].concat.apply([], PaperString) as SankeyLink[]).reduce<SankeyLinkExtended[]>((TargetABs, onePaper) => {
    //@ts-ignore
    const targetabOnePaper = TargetABs.find(
        (r) => r.source === onePaper.source && r.target === onePaper.target && r.value === onePaper.value && r.valueid === onePaper.valueid && r.process === onePaper.process
    );

    if (!targetabOnePaper) {
        //@ts-ignore
        TargetABs.push({
            source: onePaper.source,
            target: onePaper.target,
            value: onePaper.value,
            valueid: onePaper.valueid,
            paperName: onePaper.paperName,
            process: onePaper.process,
            imgUrl: onePaper.imgUrl,
            id: onePaper.id,
        });
    } else {
        targetabOnePaper.value += 1;
    }
    return TargetABs;
}, []);

//@ts-ignore
const TargetBAs = ([].concat.apply([], PaperString) as SankeyLink[]).reduce<SankeyLinkExtended[]>((TargetBAs, onePaper) => {
    //@ts-ignore
    const targetbaOnePaper = TargetBAs.find(
        (r) => r.source === onePaper.source && r.target === onePaper.target && r.value === onePaper.value && r.valueid === onePaper.valueid && r.process === onePaper.process
    );

    if (!targetbaOnePaper) {
        //@ts-ignore
        TargetBAs.push({
            source: onePaper.source,
            target: onePaper.target,
            value: onePaper.value,
            valueid: onePaper.valueid,
            paperName: onePaper.paperName,
            process: onePaper.process,
            imgUrl: onePaper.imgUrl,
            id: onePaper.id,
        });
    } else {
        targetbaOnePaper.value += 1;
    }
    return TargetBAs;
}, []);

//@ts-ignore
const TargetBBs = ([].concat.apply([], PaperString) as SankeyLink[]).reduce<SankeyLinkExtended[]>((TargetBBs, onePaper) => {
    //@ts-ignore
    const targetbbOnePaper = TargetBBs.find(
        (r) => r.source === onePaper.source && r.target === onePaper.target && r.value === onePaper.value && r.valueid === onePaper.valueid && r.process === onePaper.process
    );

    if (!targetbbOnePaper) {
        //@ts-ignore
        TargetBBs.push({
            source: onePaper.source,
            target: onePaper.target,
            value: onePaper.value,
            valueid: onePaper.valueid,
            paperName: onePaper.paperName,
            process: onePaper.process,
            imgUrl: onePaper.imgUrl,
            id: onePaper.id,
        });
    } else {
        targetbbOnePaper.value += 1;
    }
    return TargetBBs;
}, []);

//@ts-ignore
const TargetCAs = ([].concat.apply([], PaperString) as SankeyLink[]).reduce<SankeyLinkExtended[]>((TargetCAs, onePaper) => {
    //@ts-ignore
    const targetcaOnePaper = TargetCAs.find(
        (r) => r.source === onePaper.source && r.target === onePaper.target && r.value === onePaper.value && r.valueid === onePaper.valueid && r.process === onePaper.process
    );

    if (!targetcaOnePaper) {
        //@ts-ignore
        TargetCAs.push({
            source: onePaper.source,
            target: onePaper.target,
            value: onePaper.value,
            valueid: onePaper.valueid,
            paperName: onePaper.paperName,
            process: onePaper.process,
            imgUrl: onePaper.imgUrl,
            id: onePaper.id,
        });
    } else {
        targetcaOnePaper.value += 1;
    }
    return TargetCAs;
}, []);

//@ts-ignore
const TargetCBs = ([].concat.apply([], PaperString) as SankeyLink[]).reduce<SankeyLinkExtended[]>((TargetCBs, onePaper) => {
    //@ts-ignore
    const targetcbOnePaper = TargetCBs.find(
        (r) => r.source === onePaper.source && r.target === onePaper.target && r.value === onePaper.value && r.valueid === onePaper.valueid && r.process === onePaper.process
    );

    if (!targetcbOnePaper) {
        //@ts-ignore
        TargetCBs.push({
            source: onePaper.source,
            target: onePaper.target,
            value: onePaper.value,
            valueid: onePaper.valueid,
            paperName: onePaper.paperName,
            process: onePaper.process,
            imgUrl: onePaper.imgUrl,
            id: onePaper.id,
        });
    } else {
        targetcbOnePaper.value += 1;
    }
    return TargetCBs;
}, []);

//@ts-ignore
const RepAs = ([].concat.apply([], PaperString) as SankeyLink[]).reduce<SankeyLinkExtended[]>((RepAs, onePaper) => {
    //@ts-ignore
    const repaOnePaper = RepAs.find(
        (r) => r.source === onePaper.source && r.target === onePaper.target && r.value === onePaper.value && r.valueid === onePaper.valueid && r.process === onePaper.process
    );

    if (!repaOnePaper) {
        //@ts-ignore
        RepAs.push({
            source: onePaper.source,
            target: onePaper.target,
            value: onePaper.value,
            valueid: onePaper.valueid,
            paperName: onePaper.paperName,
            process: onePaper.process,
            imgUrl: onePaper.imgUrl,
            id: onePaper.id,
        });
    } else {
        repaOnePaper.value += 1;
    }
    return RepAs;
}, []);

//@ts-ignore
const RepBs = ([].concat.apply([], PaperString) as SankeyLink[]).reduce<SankeyLinkExtended[]>((RepBs, onePaper) => {
    // @ts-ignore
    const repbOnePaper = RepBs.find(
        (r) => r.source === onePaper.source && r.target === onePaper.target && r.value === onePaper.value && r.valueid === onePaper.valueid && r.process === onePaper.process
    );
    // const repbOnePaper = RepBs.find((r) => r.source === onePaper.source && r.target === onePaper.target && r.value === onePaper.value && (r.valueid === onePaper.valueid) === 'repb');
    // const repb = result.find((r) => r.source === value.source && r.target === value.target && r.valueid === 'repb'); //흐름 보이는데 이건 절대아님.
    //@ts-ignore
    // if (repb) result.push({ source: value.source, target: value.target, value: value.value, valueid: value.valueid });

    if (!repbOnePaper) {
        // console.log('onePaper.paperName', onePaper.paperName);
        //@ts-ignore
        RepBs.push({
            source: onePaper.source,
            target: onePaper.target,
            value: onePaper.value,
            valueid: onePaper.valueid,
            paperName: onePaper.paperName,
            process: onePaper.process,
            imgUrl: onePaper.imgUrl,
            id: onePaper.id,
        });
    } else {
        repbOnePaper.value += 1;
    }
    return RepBs;
}, []);
// console.log('RepBs', RepBs); // 이거 지금 100~149 노드에서 나오는 모든 링크 차례대로 정리되고 있는 상태이다.

//@ts-ignore
const RepCs = ([].concat.apply([], PaperString) as SankeyLink[]).reduce<SankeyLinkExtended[]>((RepCs, onePaper) => {
    //@ts-ignore
    const repcOnePaper = RepCs.find(
        (r) => r.source === onePaper.source && r.target === onePaper.target && r.value === onePaper.value && r.valueid === onePaper.valueid && r.process === onePaper.process
    );

    if (!repcOnePaper) {
        //@ts-ignore
        RepCs.push({
            source: onePaper.source,
            target: onePaper.target,
            value: onePaper.value,
            valueid: onePaper.valueid,
            paperName: onePaper.paperName,
            process: onePaper.process,
            imgUrl: onePaper.imgUrl,
            id: onePaper.id,
        });
    } else {
        repcOnePaper.value += 1;
    }
    return RepCs;
}, []);

//@ts-ignore
const RepDs = ([].concat.apply([], PaperString) as SankeyLink[]).reduce<SankeyLinkExtended[]>((RepDs, onePaper) => {
    //@ts-ignore
    const repdOnePaper = RepDs.find(
        (r) => r.source === onePaper.source && r.target === onePaper.target && r.value === onePaper.value && r.valueid === onePaper.valueid && r.process === onePaper.process
    );

    if (!repdOnePaper) {
        //@ts-ignore
        RepDs.push({
            source: onePaper.source,
            target: onePaper.target,
            value: onePaper.value,
            valueid: onePaper.valueid,
            paperName: onePaper.paperName,
            process: onePaper.process,
            imgUrl: onePaper.imgUrl,
            id: onePaper.id,
        });
    } else {
        repdOnePaper.value += 1;
    }
    return RepDs;
}, []);

//@ts-ignore
const RepEAs = ([].concat.apply([], PaperString) as SankeyLink[]).reduce<SankeyLinkExtended[]>((RepEAs, onePaper) => {
    //@ts-ignore
    const repeaOnePaper = RepEAs.find(
        (r) => r.source === onePaper.source && r.target === onePaper.target && r.value === onePaper.value && r.valueid === onePaper.valueid && r.process === onePaper.process
    );

    if (!repeaOnePaper) {
        //@ts-ignore
        RepEAs.push({
            source: onePaper.source,
            target: onePaper.target,
            value: onePaper.value,
            valueid: onePaper.valueid,
            paperName: onePaper.paperName,
            process: onePaper.process,
            imgUrl: onePaper.imgUrl,
            id: onePaper.id,
        });
    } else {
        repeaOnePaper.value += 1;
    }
    return RepEAs;
}, []);

//@ts-ignore
const RepEBs = ([].concat.apply([], PaperString) as SankeyLink[]).reduce<SankeyLinkExtended[]>((RepEBs, onePaper) => {
    //@ts-ignore
    const repebOnePaper = RepEBs.find(
        (r) => r.source === onePaper.source && r.target === onePaper.target && r.value === onePaper.value && r.valueid === onePaper.valueid && r.process === onePaper.process
    );

    if (!repebOnePaper) {
        //@ts-ignore
        RepEBs.push({
            source: onePaper.source,
            target: onePaper.target,
            value: onePaper.value,
            valueid: onePaper.valueid,
            paperName: onePaper.paperName,
            process: onePaper.process,
            imgUrl: onePaper.imgUrl,
            id: onePaper.id,
        });
    } else {
        repebOnePaper.value += 1;
    }
    return RepEBs;
}, []);

//@ts-ignore
const RepFs = ([].concat.apply([], PaperString) as SankeyLink[]).reduce<SankeyLinkExtended[]>((RepFs, onePaper) => {
    //@ts-ignore
    const repfOnePaper = RepFs.find(
        (r) => r.source === onePaper.source && r.target === onePaper.target && r.value === onePaper.value && r.valueid === onePaper.valueid && r.process === onePaper.process
    );

    if (!repfOnePaper) {
        //@ts-ignore
        RepFs.push({
            source: onePaper.source,
            target: onePaper.target,
            value: onePaper.value,
            valueid: onePaper.valueid,
            paperName: onePaper.paperName,
            process: onePaper.process,
            imgUrl: onePaper.imgUrl,
            id: onePaper.id,
        });
    } else {
        repfOnePaper.value += 1;
    }
    return RepFs;
}, []);

//@ts-ignore
const RepGs = ([].concat.apply([], PaperString) as SankeyLink[]).reduce<SankeyLinkExtended[]>((RepGs, onePaper) => {
    //@ts-ignore
    const repgOnePaper = RepGs.find(
        (r) => r.source === onePaper.source && r.target === onePaper.target && r.value === onePaper.value && r.valueid === onePaper.valueid && r.process === onePaper.process
    );

    if (!repgOnePaper) {
        //@ts-ignore
        RepGs.push({
            source: onePaper.source,
            target: onePaper.target,
            value: onePaper.value,
            valueid: onePaper.valueid,
            paperName: onePaper.paperName,
            process: onePaper.process,
            imgUrl: onePaper.imgUrl,
            id: onePaper.id,
        });
    } else {
        repgOnePaper.value += 1;
    }
    return RepGs;
}, []);

//@ts-ignore
const RepHs = ([].concat.apply([], PaperString) as SankeyLink[]).reduce<SankeyLinkExtended[]>((RepHs, onePaper) => {
    //@ts-ignore
    const rephOnePaper = RepHs.find(
        (r) => r.source === onePaper.source && r.target === onePaper.target && r.value === onePaper.value && r.valueid === onePaper.valueid && r.process === onePaper.process
    );

    if (!rephOnePaper) {
        //@ts-ignore
        RepHs.push({
            source: onePaper.source,
            target: onePaper.target,
            value: onePaper.value,
            valueid: onePaper.valueid,
            paperName: onePaper.paperName,
            process: onePaper.process,
            imgUrl: onePaper.imgUrl,
            id: onePaper.id,
        });
    } else {
        rephOnePaper.value += 1;
    }
    return RepHs;
}, []);

//@ts-ignore
const ClusterOnes = ([].concat.apply([], ClusteroneLinkData) as SankeyLink[]).reduce<SankeyLinkExtended[]>((ClusterOnes, onePaper) => {
    //@ts-ignore
    const clusterOnePaper = ClusterOnes.find(
        (r) => r.source === onePaper.source && r.target === onePaper.target && r.value === onePaper.value && r.valueid === onePaper.valueid && r.process === onePaper.process
    );

    if (!clusterOnePaper) {
        //@ts-ignore
        ClusterOnes.push({
            source: onePaper.source,
            target: onePaper.target,
            value: onePaper.value,
            valueid: onePaper.valueid,
            paperName: onePaper.paperName,
            process: onePaper.process,
            imgUrl: onePaper.imgUrl,
            id: onePaper.id,
            cluster: onePaper.cluster,
        });
    } else {
        clusterOnePaper.value += 1;
    }
    return ClusterOnes;
}, []);

console.log(ClusterOnes);

//@ts-ignore
const ClusterTwos = ([].concat.apply([], ClustertwoLinkData) as SankeyLink[]).reduce<SankeyLinkExtended[]>((ClusterTwos, onePaper) => {
    //@ts-ignore
    const clustertwoPaper = ClusterTwos.find(
        (r) => r.source === onePaper.source && r.target === onePaper.target && r.value === onePaper.value && r.valueid === onePaper.valueid && r.process === onePaper.process
    );

    if (!clustertwoPaper) {
        //@ts-ignore
        ClusterTwos.push({
            source: onePaper.source,
            target: onePaper.target,
            value: onePaper.value,
            valueid: onePaper.valueid,
            paperName: onePaper.paperName,
            process: onePaper.process,
            imgUrl: onePaper.imgUrl,
            id: onePaper.id,
            cluster: onePaper.cluster,
        });
    } else {
        clustertwoPaper.value += 1;
    }
    return ClusterTwos;
}, []);

//@ts-ignore
const ClusterThrees = ([].concat.apply([], ClusterthreeLinkData) as SankeyLink[]).reduce<SankeyLinkExtended[]>((ClusterThrees, onePaper) => {
    //@ts-ignore
    const clusterThreePaper = ClusterThrees.find(
        (r) => r.source === onePaper.source && r.target === onePaper.target && r.value === onePaper.value && r.valueid === onePaper.valueid && r.process === onePaper.process
    );

    if (!clusterThreePaper) {
        //@ts-ignore
        ClusterThrees.push({
            source: onePaper.source,
            target: onePaper.target,
            value: onePaper.value,
            valueid: onePaper.valueid,
            paperName: onePaper.paperName,
            process: onePaper.process,
            imgUrl: onePaper.imgUrl,
            id: onePaper.id,
            cluster: onePaper.cluster,
        });
    } else {
        clusterThreePaper.value += 1;
    }
    return ClusterThrees;
}, []);

//@ts-ignore
const ClusterFours = ([].concat.apply([], ClusterfourLinkData) as SankeyLink[]).reduce<SankeyLinkExtended[]>((ClusterFours, onePaper) => {
    //@ts-ignore
    const clusterFourPaper = ClusterFours.find(
        (r) => r.source === onePaper.source && r.target === onePaper.target && r.value === onePaper.value && r.valueid === onePaper.valueid && r.process === onePaper.process
    );

    if (!clusterFourPaper) {
        //@ts-ignore
        ClusterFours.push({
            source: onePaper.source,
            target: onePaper.target,
            value: onePaper.value,
            valueid: onePaper.valueid,
            paperName: onePaper.paperName,
            process: onePaper.process,
            imgUrl: onePaper.imgUrl,
            id: onePaper.id,
            cluster: onePaper.cluster,
        });
    } else {
        clusterFourPaper.value += 1;
    }
    return ClusterFours;
}, []);

// console.log(ClusterFours);
//@ts-ignore
const Int1s = ([].concat.apply([], Int1Data) as SankeyLink[]).reduce<SankeyLinkExtended[]>((Int1s, onePaper) => {
    //@ts-ignore
    const clusterFourPaper = Int1s.find(
        (r) => r.source === onePaper.source && r.target === onePaper.target && r.value === onePaper.value && r.valueid === onePaper.valueid && r.process === onePaper.process
    );

    if (!clusterFourPaper) {
        //@ts-ignore
        Int1s.push({
            source: onePaper.source,
            target: onePaper.target,
            value: onePaper.value,
            valueid: onePaper.valueid,
            paperName: onePaper.paperName,
            process: onePaper.process,
            imgUrl: onePaper.imgUrl,
            id: onePaper.id,
            netnode: onePaper.netnode,
        });
    } else {
        clusterFourPaper.value += 1;
    }
    return Int1s;
}, []);

//@ts-ignore
const Int2s = ([].concat.apply([], Int2Data) as SankeyLink[]).reduce<SankeyLinkExtended[]>((Int2s, onePaper) => {
    //@ts-ignore
    const clusterFourPaper = Int2s.find(
        (r) => r.source === onePaper.source && r.target === onePaper.target && r.value === onePaper.value && r.valueid === onePaper.valueid && r.process === onePaper.process
    );

    if (!clusterFourPaper) {
        //@ts-ignore
        Int2s.push({
            source: onePaper.source,
            target: onePaper.target,
            value: onePaper.value,
            valueid: onePaper.valueid,
            paperName: onePaper.paperName,
            process: onePaper.process,
            imgUrl: onePaper.imgUrl,
            id: onePaper.id,
            netnode: onePaper.netnode,
        });
    } else {
        clusterFourPaper.value += 1;
    }
    return Int2s;
}, []);

//@ts-ignore
const Int3s = ([].concat.apply([], Int3Data) as SankeyLink[]).reduce<SankeyLinkExtended[]>((Int3s, onePaper) => {
    //@ts-ignore
    const clusterFourPaper = Int3s.find(
        (r) => r.source === onePaper.source && r.target === onePaper.target && r.value === onePaper.value && r.valueid === onePaper.valueid && r.process === onePaper.process
    );

    if (!clusterFourPaper) {
        //@ts-ignore
        Int3s.push({
            source: onePaper.source,
            target: onePaper.target,
            value: onePaper.value,
            valueid: onePaper.valueid,
            paperName: onePaper.paperName,
            process: onePaper.process,
            imgUrl: onePaper.imgUrl,
            id: onePaper.id,
            netnode: onePaper.netnode,
        });
    } else {
        clusterFourPaper.value += 1;
    }
    return Int3s;
}, []);

//@ts-ignore
const Int4s = ([].concat.apply([], Int4Data) as SankeyLink[]).reduce<SankeyLinkExtended[]>((Int4s, onePaper) => {
    //@ts-ignore
    const clusterFourPaper = Int4s.find(
        (r) => r.source === onePaper.source && r.target === onePaper.target && r.value === onePaper.value && r.valueid === onePaper.valueid && r.process === onePaper.process
    );

    if (!clusterFourPaper) {
        //@ts-ignore
        Int4s.push({
            source: onePaper.source,
            target: onePaper.target,
            value: onePaper.value,
            valueid: onePaper.valueid,
            paperName: onePaper.paperName,
            process: onePaper.process,
            imgUrl: onePaper.imgUrl,
            id: onePaper.id,
            netnode: onePaper.netnode,
        });
    } else {
        clusterFourPaper.value += 1;
    }
    return Int4s;
}, []);

//@ts-ignore
const Int5s = ([].concat.apply([], Int5Data) as SankeyLink[]).reduce<SankeyLinkExtended[]>((Int5s, onePaper) => {
    //@ts-ignore
    const clusterFourPaper = Int5s.find(
        (r) => r.source === onePaper.source && r.target === onePaper.target && r.value === onePaper.value && r.valueid === onePaper.valueid && r.process === onePaper.process
    );

    if (!clusterFourPaper) {
        //@ts-ignore
        Int5s.push({
            source: onePaper.source,
            target: onePaper.target,
            value: onePaper.value,
            valueid: onePaper.valueid,
            paperName: onePaper.paperName,
            process: onePaper.process,
            imgUrl: onePaper.imgUrl,
            id: onePaper.id,
            netnode: onePaper.netnode,
        });
    } else {
        clusterFourPaper.value += 1;
    }
    return Int5s;
}, []);

//@ts-ignore
const Int6s = ([].concat.apply([], Int6Data) as SankeyLink[]).reduce<SankeyLinkExtended[]>((Int6s, onePaper) => {
    //@ts-ignore
    const clusterFourPaper = Int6s.find(
        (r) => r.source === onePaper.source && r.target === onePaper.target && r.value === onePaper.value && r.valueid === onePaper.valueid && r.process === onePaper.process
    );

    if (!clusterFourPaper) {
        //@ts-ignore
        Int6s.push({
            source: onePaper.source,
            target: onePaper.target,
            value: onePaper.value,
            valueid: onePaper.valueid,
            paperName: onePaper.paperName,
            process: onePaper.process,
            imgUrl: onePaper.imgUrl,
            id: onePaper.id,
            netnode: onePaper.netnode,
        });
    } else {
        clusterFourPaper.value += 1;
    }
    return Int6s;
}, []);

//@ts-ignore
const Int7s = ([].concat.apply([], Int7Data) as SankeyLink[]).reduce<SankeyLinkExtended[]>((Int7s, onePaper) => {
    //@ts-ignore
    const clusterFourPaper = Int7s.find(
        (r) => r.source === onePaper.source && r.target === onePaper.target && r.value === onePaper.value && r.valueid === onePaper.valueid && r.process === onePaper.process
    );

    if (!clusterFourPaper) {
        //@ts-ignore
        Int7s.push({
            source: onePaper.source,
            target: onePaper.target,
            value: onePaper.value,
            valueid: onePaper.valueid,
            paperName: onePaper.paperName,
            process: onePaper.process,
            imgUrl: onePaper.imgUrl,
            id: onePaper.id,
            netnode: onePaper.netnode,
        });
    } else {
        clusterFourPaper.value += 1;
    }
    return Int7s;
}, []);

//@ts-ignore
const Int8s = ([].concat.apply([], Int8Data) as SankeyLink[]).reduce<SankeyLinkExtended[]>((Int8s, onePaper) => {
    //@ts-ignore
    const clusterFourPaper = Int8s.find(
        (r) => r.source === onePaper.source && r.target === onePaper.target && r.value === onePaper.value && r.valueid === onePaper.valueid && r.process === onePaper.process
    );

    if (!clusterFourPaper) {
        //@ts-ignore
        Int8s.push({
            source: onePaper.source,
            target: onePaper.target,
            value: onePaper.value,
            valueid: onePaper.valueid,
            paperName: onePaper.paperName,
            process: onePaper.process,
            imgUrl: onePaper.imgUrl,
            id: onePaper.id,
            netnode: onePaper.netnode,
        });
    } else {
        clusterFourPaper.value += 1;
    }
    return Int8s;
}, []);

//@ts-ignore
const Int9s = ([].concat.apply([], Int9Data) as SankeyLink[]).reduce<SankeyLinkExtended[]>((Int9s, onePaper) => {
    //@ts-ignore
    const clusterFourPaper = Int9s.find(
        (r) => r.source === onePaper.source && r.target === onePaper.target && r.value === onePaper.value && r.valueid === onePaper.valueid && r.process === onePaper.process
    );

    if (!clusterFourPaper) {
        //@ts-ignore
        Int9s.push({
            source: onePaper.source,
            target: onePaper.target,
            value: onePaper.value,
            valueid: onePaper.valueid,
            paperName: onePaper.paperName,
            process: onePaper.process,
            imgUrl: onePaper.imgUrl,
            id: onePaper.id,
            netnode: onePaper.netnode,
        });
    } else {
        clusterFourPaper.value += 1;
    }
    return Int9s;
}, []);

//@ts-ignore
const Int10s = ([].concat.apply([], Int10Data) as SankeyLink[]).reduce<SankeyLinkExtended[]>((Int10s, onePaper) => {
    //@ts-ignore
    const clusterFourPaper = Int10s.find(
        (r) => r.source === onePaper.source && r.target === onePaper.target && r.value === onePaper.value && r.valueid === onePaper.valueid && r.process === onePaper.process
    );

    if (!clusterFourPaper) {
        //@ts-ignore
        Int10s.push({
            source: onePaper.source,
            target: onePaper.target,
            value: onePaper.value,
            valueid: onePaper.valueid,
            paperName: onePaper.paperName,
            process: onePaper.process,
            imgUrl: onePaper.imgUrl,
            id: onePaper.id,
            netnode: onePaper.netnode,
        });
    } else {
        clusterFourPaper.value += 1;
    }
    return Int10s;
}, []);

//@ts-ignore
const Int11s = ([].concat.apply([], Int11Data) as SankeyLink[]).reduce<SankeyLinkExtended[]>((Int11s, onePaper) => {
    //@ts-ignore
    const clusterFourPaper = Int11s.find(
        (r) => r.source === onePaper.source && r.target === onePaper.target && r.value === onePaper.value && r.valueid === onePaper.valueid && r.process === onePaper.process
    );

    if (!clusterFourPaper) {
        //@ts-ignore
        Int11s.push({
            source: onePaper.source,
            target: onePaper.target,
            value: onePaper.value,
            valueid: onePaper.valueid,
            paperName: onePaper.paperName,
            process: onePaper.process,
            imgUrl: onePaper.imgUrl,
            id: onePaper.id,
            netnode: onePaper.netnode,
        });
    } else {
        clusterFourPaper.value += 1;
    }
    return Int11s;
}, []);

//@ts-ignore
const Int12s = ([].concat.apply([], Int12Data) as SankeyLink[]).reduce<SankeyLinkExtended[]>((Int12s, onePaper) => {
    //@ts-ignore
    const clusterFourPaper = Int12s.find(
        (r) => r.source === onePaper.source && r.target === onePaper.target && r.value === onePaper.value && r.valueid === onePaper.valueid && r.process === onePaper.process
    );

    if (!clusterFourPaper) {
        //@ts-ignore
        Int12s.push({
            source: onePaper.source,
            target: onePaper.target,
            value: onePaper.value,
            valueid: onePaper.valueid,
            paperName: onePaper.paperName,
            process: onePaper.process,
            imgUrl: onePaper.imgUrl,
            id: onePaper.id,
            netnode: onePaper.netnode,
        });
    } else {
        clusterFourPaper.value += 1;
    }
    return Int12s;
}, []);

//@ts-ignore
const Int13s = ([].concat.apply([], Int13Data) as SankeyLink[]).reduce<SankeyLinkExtended[]>((Int13s, onePaper) => {
    //@ts-ignore
    const clusterFourPaper = Int13s.find(
        (r) => r.source === onePaper.source && r.target === onePaper.target && r.value === onePaper.value && r.valueid === onePaper.valueid && r.process === onePaper.process
    );

    if (!clusterFourPaper) {
        //@ts-ignore
        Int13s.push({
            source: onePaper.source,
            target: onePaper.target,
            value: onePaper.value,
            valueid: onePaper.valueid,
            paperName: onePaper.paperName,
            process: onePaper.process,
            imgUrl: onePaper.imgUrl,
            id: onePaper.id,
            netnode: onePaper.netnode,
        });
    } else {
        clusterFourPaper.value += 1;
    }
    return Int13s;
}, []);

//@ts-ignore
const Int14s = ([].concat.apply([], Int14Data) as SankeyLink[]).reduce<SankeyLinkExtended[]>((Int14s, onePaper) => {
    //@ts-ignore
    const clusterFourPaper = Int14s.find(
        (r) => r.source === onePaper.source && r.target === onePaper.target && r.value === onePaper.value && r.valueid === onePaper.valueid && r.process === onePaper.process
    );

    if (!clusterFourPaper) {
        //@ts-ignore
        Int14s.push({
            source: onePaper.source,
            target: onePaper.target,
            value: onePaper.value,
            valueid: onePaper.valueid,
            paperName: onePaper.paperName,
            process: onePaper.process,
            imgUrl: onePaper.imgUrl,
            id: onePaper.id,
            netnode: onePaper.netnode,
        });
    } else {
        clusterFourPaper.value += 1;
    }
    return Int14s;
}, []);

//@ts-ignore
const Int15s = ([].concat.apply([], Int15Data) as SankeyLink[]).reduce<SankeyLinkExtended[]>((Int15s, onePaper) => {
    //@ts-ignore
    const clusterFourPaper = Int15s.find(
        (r) => r.source === onePaper.source && r.target === onePaper.target && r.value === onePaper.value && r.valueid === onePaper.valueid && r.process === onePaper.process
    );

    if (!clusterFourPaper) {
        //@ts-ignore
        Int15s.push({
            source: onePaper.source,
            target: onePaper.target,
            value: onePaper.value,
            valueid: onePaper.valueid,
            paperName: onePaper.paperName,
            process: onePaper.process,
            imgUrl: onePaper.imgUrl,
            id: onePaper.id,
            netnode: onePaper.netnode,
        });
    } else {
        clusterFourPaper.value += 1;
    }
    return Int15s;
}, []);

//@ts-ignore
const Int16s = ([].concat.apply([], Int16Data) as SankeyLink[]).reduce<SankeyLinkExtended[]>((Int16s, onePaper) => {
    //@ts-ignore
    const clusterFourPaper = Int16s.find(
        (r) => r.source === onePaper.source && r.target === onePaper.target && r.value === onePaper.value && r.valueid === onePaper.valueid && r.process === onePaper.process
    );

    if (!clusterFourPaper) {
        //@ts-ignore
        Int16s.push({
            source: onePaper.source,
            target: onePaper.target,
            value: onePaper.value,
            valueid: onePaper.valueid,
            paperName: onePaper.paperName,
            process: onePaper.process,
            imgUrl: onePaper.imgUrl,
            id: onePaper.id,
            netnode: onePaper.netnode,
        });
    } else {
        clusterFourPaper.value += 1;
    }
    return Int16s;
}, []);

//@ts-ignore
const Rep1s = ([].concat.apply([], Rep1Data) as SankeyLink[]).reduce<SankeyLinkExtended[]>((Rep1s, onePaper) => {
    //@ts-ignore
    const clusterFourPaper = Rep1s.find(
        (r) => r.source === onePaper.source && r.target === onePaper.target && r.value === onePaper.value && r.valueid === onePaper.valueid && r.process === onePaper.process
    );

    if (!clusterFourPaper) {
        //@ts-ignore
        Rep1s.push({
            source: onePaper.source,
            target: onePaper.target,
            value: onePaper.value,
            valueid: onePaper.valueid,
            paperName: onePaper.paperName,
            process: onePaper.process,
            imgUrl: onePaper.imgUrl,
            id: onePaper.id,
            netnode: onePaper.netnode,
        });
    } else {
        clusterFourPaper.value += 1;
    }
    return Rep1s;
}, []);

//@ts-ignore
const Rep2s = ([].concat.apply([], Rep2Data) as SankeyLink[]).reduce<SankeyLinkExtended[]>((Rep2s, onePaper) => {
    //@ts-ignore
    const clusterFourPaper = Rep2s.find(
        (r) => r.source === onePaper.source && r.target === onePaper.target && r.value === onePaper.value && r.valueid === onePaper.valueid && r.process === onePaper.process
    );

    if (!clusterFourPaper) {
        //@ts-ignore
        Rep2s.push({
            source: onePaper.source,
            target: onePaper.target,
            value: onePaper.value,
            valueid: onePaper.valueid,
            paperName: onePaper.paperName,
            process: onePaper.process,
            imgUrl: onePaper.imgUrl,
            id: onePaper.id,
            netnode: onePaper.netnode,
        });
    } else {
        clusterFourPaper.value += 1;
    }
    return Rep2s;
}, []);

//@ts-ignore
const Rep3s = ([].concat.apply([], Rep3Data) as SankeyLink[]).reduce<SankeyLinkExtended[]>((Rep3s, onePaper) => {
    //@ts-ignore
    const clusterFourPaper = Rep3s.find(
        (r) => r.source === onePaper.source && r.target === onePaper.target && r.value === onePaper.value && r.valueid === onePaper.valueid && r.process === onePaper.process
    );

    if (!clusterFourPaper) {
        //@ts-ignore
        Rep3s.push({
            source: onePaper.source,
            target: onePaper.target,
            value: onePaper.value,
            valueid: onePaper.valueid,
            paperName: onePaper.paperName,
            process: onePaper.process,
            imgUrl: onePaper.imgUrl,
            id: onePaper.id,
            netnode: onePaper.netnode,
        });
    } else {
        clusterFourPaper.value += 1;
    }
    return Rep3s;
}, []);

//@ts-ignore
const Rep4s = ([].concat.apply([], Rep4Data) as SankeyLink[]).reduce<SankeyLinkExtended[]>((Rep4s, onePaper) => {
    //@ts-ignore
    const clusterFourPaper = Rep4s.find(
        (r) => r.source === onePaper.source && r.target === onePaper.target && r.value === onePaper.value && r.valueid === onePaper.valueid && r.process === onePaper.process
    );

    if (!clusterFourPaper) {
        //@ts-ignore
        Rep4s.push({
            source: onePaper.source,
            target: onePaper.target,
            value: onePaper.value,
            valueid: onePaper.valueid,
            paperName: onePaper.paperName,
            process: onePaper.process,
            imgUrl: onePaper.imgUrl,
            id: onePaper.id,
            netnode: onePaper.netnode,
        });
    } else {
        clusterFourPaper.value += 1;
    }
    return Rep4s;
}, []);

//@ts-ignore
const Rep5s = ([].concat.apply([], Rep5Data) as SankeyLink[]).reduce<SankeyLinkExtended[]>((Rep5s, onePaper) => {
    //@ts-ignore
    const clusterFourPaper = Rep5s.find(
        (r) => r.source === onePaper.source && r.target === onePaper.target && r.value === onePaper.value && r.valueid === onePaper.valueid && r.process === onePaper.process
    );

    if (!clusterFourPaper) {
        //@ts-ignore
        Rep5s.push({
            source: onePaper.source,
            target: onePaper.target,
            value: onePaper.value,
            valueid: onePaper.valueid,
            paperName: onePaper.paperName,
            process: onePaper.process,
            imgUrl: onePaper.imgUrl,
            id: onePaper.id,
            netnode: onePaper.netnode,
        });
    } else {
        clusterFourPaper.value += 1;
    }
    return Rep5s;
}, []);

//@ts-ignore
const Rep6s = ([].concat.apply([], Rep6Data) as SankeyLink[]).reduce<SankeyLinkExtended[]>((Rep6s, onePaper) => {
    //@ts-ignore
    const clusterFourPaper = Rep6s.find(
        (r) => r.source === onePaper.source && r.target === onePaper.target && r.value === onePaper.value && r.valueid === onePaper.valueid && r.process === onePaper.process
    );

    if (!clusterFourPaper) {
        //@ts-ignore
        Rep6s.push({
            source: onePaper.source,
            target: onePaper.target,
            value: onePaper.value,
            valueid: onePaper.valueid,
            paperName: onePaper.paperName,
            process: onePaper.process,
            imgUrl: onePaper.imgUrl,
            id: onePaper.id,
            netnode: onePaper.netnode,
        });
    } else {
        clusterFourPaper.value += 1;
    }
    return Rep6s;
}, []);

//@ts-ignore
const Rep7s = ([].concat.apply([], Rep7Data) as SankeyLink[]).reduce<SankeyLinkExtended[]>((Rep7s, onePaper) => {
    //@ts-ignore
    const clusterFourPaper = Rep7s.find(
        (r) => r.source === onePaper.source && r.target === onePaper.target && r.value === onePaper.value && r.valueid === onePaper.valueid && r.process === onePaper.process
    );

    if (!clusterFourPaper) {
        //@ts-ignore
        Rep7s.push({
            source: onePaper.source,
            target: onePaper.target,
            value: onePaper.value,
            valueid: onePaper.valueid,
            paperName: onePaper.paperName,
            process: onePaper.process,
            imgUrl: onePaper.imgUrl,
            id: onePaper.id,
            netnode: onePaper.netnode,
        });
    } else {
        clusterFourPaper.value += 1;
    }
    return Rep7s;
}, []);

//@ts-ignore
const Rep8s = ([].concat.apply([], Rep8Data) as SankeyLink[]).reduce<SankeyLinkExtended[]>((Rep8s, onePaper) => {
    //@ts-ignore
    const clusterFourPaper = Rep8s.find(
        (r) => r.source === onePaper.source && r.target === onePaper.target && r.value === onePaper.value && r.valueid === onePaper.valueid && r.process === onePaper.process
    );

    if (!clusterFourPaper) {
        //@ts-ignore
        Rep8s.push({
            source: onePaper.source,
            target: onePaper.target,
            value: onePaper.value,
            valueid: onePaper.valueid,
            paperName: onePaper.paperName,
            process: onePaper.process,
            imgUrl: onePaper.imgUrl,
            id: onePaper.id,
            netnode: onePaper.netnode,
        });
    } else {
        clusterFourPaper.value += 1;
    }
    return Rep8s;
}, []);

//@ts-ignore
const Rep9s = ([].concat.apply([], Rep9Data) as SankeyLink[]).reduce<SankeyLinkExtended[]>((Rep9s, onePaper) => {
    //@ts-ignore
    const clusterFourPaper = Rep9s.find(
        (r) => r.source === onePaper.source && r.target === onePaper.target && r.value === onePaper.value && r.valueid === onePaper.valueid && r.process === onePaper.process
    );

    if (!clusterFourPaper) {
        //@ts-ignore
        Rep9s.push({
            source: onePaper.source,
            target: onePaper.target,
            value: onePaper.value,
            valueid: onePaper.valueid,
            paperName: onePaper.paperName,
            process: onePaper.process,
            imgUrl: onePaper.imgUrl,
            id: onePaper.id,
            netnode: onePaper.netnode,
        });
    } else {
        clusterFourPaper.value += 1;
    }
    return Rep9s;
}, []);

//@ts-ignore
const Rep10s = ([].concat.apply([], Rep10Data) as SankeyLink[]).reduce<SankeyLinkExtended[]>((Rep10s, onePaper) => {
    //@ts-ignore
    const clusterFourPaper = Rep10s.find(
        (r) => r.source === onePaper.source && r.target === onePaper.target && r.value === onePaper.value && r.valueid === onePaper.valueid && r.process === onePaper.process
    );

    if (!clusterFourPaper) {
        //@ts-ignore
        Rep10s.push({
            source: onePaper.source,
            target: onePaper.target,
            value: onePaper.value,
            valueid: onePaper.valueid,
            paperName: onePaper.paperName,
            process: onePaper.process,
            imgUrl: onePaper.imgUrl,
            id: onePaper.id,
            netnode: onePaper.netnode,
        });
    } else {
        clusterFourPaper.value += 1;
    }
    return Rep10s;
}, []);

//@ts-ignore
const Rep11s = ([].concat.apply([], Rep11Data) as SankeyLink[]).reduce<SankeyLinkExtended[]>((Rep11s, onePaper) => {
    //@ts-ignore
    const clusterFourPaper = Rep11s.find(
        (r) => r.source === onePaper.source && r.target === onePaper.target && r.value === onePaper.value && r.valueid === onePaper.valueid && r.process === onePaper.process
    );

    if (!clusterFourPaper) {
        //@ts-ignore
        Rep11s.push({
            source: onePaper.source,
            target: onePaper.target,
            value: onePaper.value,
            valueid: onePaper.valueid,
            paperName: onePaper.paperName,
            process: onePaper.process,
            imgUrl: onePaper.imgUrl,
            id: onePaper.id,
            netnode: onePaper.netnode,
        });
    } else {
        clusterFourPaper.value += 1;
    }
    return Rep11s;
}, []);

//@ts-ignore
const Rep12s = ([].concat.apply([], Rep12Data) as SankeyLink[]).reduce<SankeyLinkExtended[]>((Rep12s, onePaper) => {
    //@ts-ignore
    const clusterFourPaper = Rep12s.find(
        (r) => r.source === onePaper.source && r.target === onePaper.target && r.value === onePaper.value && r.valueid === onePaper.valueid && r.process === onePaper.process
    );

    if (!clusterFourPaper) {
        //@ts-ignore
        Rep12s.push({
            source: onePaper.source,
            target: onePaper.target,
            value: onePaper.value,
            valueid: onePaper.valueid,
            paperName: onePaper.paperName,
            process: onePaper.process,
            imgUrl: onePaper.imgUrl,
            id: onePaper.id,
            netnode: onePaper.netnode,
        });
    } else {
        clusterFourPaper.value += 1;
    }
    return Rep12s;
}, []);

//@ts-ignore
const Rep13s = ([].concat.apply([], Rep13Data) as SankeyLink[]).reduce<SankeyLinkExtended[]>((Rep13s, onePaper) => {
    //@ts-ignore
    const clusterFourPaper = Rep13s.find(
        (r) => r.source === onePaper.source && r.target === onePaper.target && r.value === onePaper.value && r.valueid === onePaper.valueid && r.process === onePaper.process
    );

    if (!clusterFourPaper) {
        //@ts-ignore
        Rep13s.push({
            source: onePaper.source,
            target: onePaper.target,
            value: onePaper.value,
            valueid: onePaper.valueid,
            paperName: onePaper.paperName,
            process: onePaper.process,
            imgUrl: onePaper.imgUrl,
            id: onePaper.id,
            netnode: onePaper.netnode,
        });
    } else {
        clusterFourPaper.value += 1;
    }
    return Rep13s;
}, []);

//@ts-ignore
const Rep14s = ([].concat.apply([], Rep14Data) as SankeyLink[]).reduce<SankeyLinkExtended[]>((Rep14s, onePaper) => {
    //@ts-ignore
    const clusterFourPaper = Rep14s.find(
        (r) => r.source === onePaper.source && r.target === onePaper.target && r.value === onePaper.value && r.valueid === onePaper.valueid && r.process === onePaper.process
    );

    if (!clusterFourPaper) {
        //@ts-ignore
        Rep14s.push({
            source: onePaper.source,
            target: onePaper.target,
            value: onePaper.value,
            valueid: onePaper.valueid,
            paperName: onePaper.paperName,
            process: onePaper.process,
            imgUrl: onePaper.imgUrl,
            id: onePaper.id,
            netnode: onePaper.netnode,
        });
    } else {
        clusterFourPaper.value += 1;
    }
    return Rep14s;
}, []);

//@ts-ignore
const Rep15s = ([].concat.apply([], Rep15Data) as SankeyLink[]).reduce<SankeyLinkExtended[]>((Rep15s, onePaper) => {
    //@ts-ignore
    const clusterFourPaper = Rep15s.find(
        (r) => r.source === onePaper.source && r.target === onePaper.target && r.value === onePaper.value && r.valueid === onePaper.valueid && r.process === onePaper.process
    );

    if (!clusterFourPaper) {
        //@ts-ignore
        Rep15s.push({
            source: onePaper.source,
            target: onePaper.target,
            value: onePaper.value,
            valueid: onePaper.valueid,
            paperName: onePaper.paperName,
            process: onePaper.process,
            imgUrl: onePaper.imgUrl,
            id: onePaper.id,
            netnode: onePaper.netnode,
        });
    } else {
        clusterFourPaper.value += 1;
    }
    return Rep15s;
}, []);

//@ts-ignore
const Rep16s = ([].concat.apply([], Rep16Data) as SankeyLink[]).reduce<SankeyLinkExtended[]>((Rep16s, onePaper) => {
    //@ts-ignore
    const clusterFourPaper = Rep16s.find(
        (r) => r.source === onePaper.source && r.target === onePaper.target && r.value === onePaper.value && r.valueid === onePaper.valueid && r.process === onePaper.process
    );

    if (!clusterFourPaper) {
        //@ts-ignore
        Rep16s.push({
            source: onePaper.source,
            target: onePaper.target,
            value: onePaper.value,
            valueid: onePaper.valueid,
            paperName: onePaper.paperName,
            process: onePaper.process,
            imgUrl: onePaper.imgUrl,
            id: onePaper.id,
            netnode: onePaper.netnode,
        });
    } else {
        clusterFourPaper.value += 1;
    }
    return Rep16s;
}, []);

//@ts-ignore
const Rep17s = ([].concat.apply([], Rep17Data) as SankeyLink[]).reduce<SankeyLinkExtended[]>((Rep17s, onePaper) => {
    //@ts-ignore
    const clusterFourPaper = Rep17s.find(
        (r) => r.source === onePaper.source && r.target === onePaper.target && r.value === onePaper.value && r.valueid === onePaper.valueid && r.process === onePaper.process
    );

    if (!clusterFourPaper) {
        //@ts-ignore
        Rep17s.push({
            source: onePaper.source,
            target: onePaper.target,
            value: onePaper.value,
            valueid: onePaper.valueid,
            paperName: onePaper.paperName,
            process: onePaper.process,
            imgUrl: onePaper.imgUrl,
            id: onePaper.id,
            netnode: onePaper.netnode,
        });
    } else {
        clusterFourPaper.value += 1;
    }
    return Rep17s;
}, []);

//@ts-ignore
const Rep18s = ([].concat.apply([], Rep18Data) as SankeyLink[]).reduce<SankeyLinkExtended[]>((Rep18s, onePaper) => {
    //@ts-ignore
    const clusterFourPaper = Rep18s.find(
        (r) => r.source === onePaper.source && r.target === onePaper.target && r.value === onePaper.value && r.valueid === onePaper.valueid && r.process === onePaper.process
    );

    if (!clusterFourPaper) {
        //@ts-ignore
        Rep18s.push({
            source: onePaper.source,
            target: onePaper.target,
            value: onePaper.value,
            valueid: onePaper.valueid,
            paperName: onePaper.paperName,
            process: onePaper.process,
            imgUrl: onePaper.imgUrl,
            id: onePaper.id,
            netnode: onePaper.netnode,
        });
    } else {
        clusterFourPaper.value += 1;
    }
    return Rep18s;
}, []);

//@ts-ignore
const Rep19s = ([].concat.apply([], Rep19Data) as SankeyLink[]).reduce<SankeyLinkExtended[]>((Rep19s, onePaper) => {
    //@ts-ignore
    const clusterFourPaper = Rep19s.find(
        (r) => r.source === onePaper.source && r.target === onePaper.target && r.value === onePaper.value && r.valueid === onePaper.valueid && r.process === onePaper.process
    );

    if (!clusterFourPaper) {
        //@ts-ignore
        Rep19s.push({
            source: onePaper.source,
            target: onePaper.target,
            value: onePaper.value,
            valueid: onePaper.valueid,
            paperName: onePaper.paperName,
            process: onePaper.process,
            imgUrl: onePaper.imgUrl,
            id: onePaper.id,
            netnode: onePaper.netnode,
        });
    } else {
        clusterFourPaper.value += 1;
    }
    return Rep19s;
}, []);

//@ts-ignore
const Rep20s = ([].concat.apply([], Rep20Data) as SankeyLink[]).reduce<SankeyLinkExtended[]>((Rep20s, onePaper) => {
    //@ts-ignore
    const clusterFourPaper = Rep20s.find(
        (r) => r.source === onePaper.source && r.target === onePaper.target && r.value === onePaper.value && r.valueid === onePaper.valueid && r.process === onePaper.process
    );

    if (!clusterFourPaper) {
        //@ts-ignore
        Rep20s.push({
            source: onePaper.source,
            target: onePaper.target,
            value: onePaper.value,
            valueid: onePaper.valueid,
            paperName: onePaper.paperName,
            process: onePaper.process,
            imgUrl: onePaper.imgUrl,
            id: onePaper.id,
            netnode: onePaper.netnode,
        });
    } else {
        clusterFourPaper.value += 1;
    }
    return Rep20s;
}, []);

//@ts-ignore
const Rep21s = ([].concat.apply([], Rep21Data) as SankeyLink[]).reduce<SankeyLinkExtended[]>((Rep21s, onePaper) => {
    //@ts-ignore
    const clusterFourPaper = Rep21s.find(
        (r) => r.source === onePaper.source && r.target === onePaper.target && r.value === onePaper.value && r.valueid === onePaper.valueid && r.process === onePaper.process
    );

    if (!clusterFourPaper) {
        //@ts-ignore
        Rep21s.push({
            source: onePaper.source,
            target: onePaper.target,
            value: onePaper.value,
            valueid: onePaper.valueid,
            paperName: onePaper.paperName,
            process: onePaper.process,
            imgUrl: onePaper.imgUrl,
            id: onePaper.id,
            netnode: onePaper.netnode,
        });
    } else {
        clusterFourPaper.value += 1;
    }
    return Rep21s;
}, []);

//@ts-ignore
const Rep22s = ([].concat.apply([], Rep22Data) as SankeyLink[]).reduce<SankeyLinkExtended[]>((Rep22s, onePaper) => {
    //@ts-ignore
    const clusterFourPaper = Rep22s.find(
        (r) => r.source === onePaper.source && r.target === onePaper.target && r.value === onePaper.value && r.valueid === onePaper.valueid && r.process === onePaper.process
    );

    if (!clusterFourPaper) {
        //@ts-ignore
        Rep22s.push({
            source: onePaper.source,
            target: onePaper.target,
            value: onePaper.value,
            valueid: onePaper.valueid,
            paperName: onePaper.paperName,
            process: onePaper.process,
            imgUrl: onePaper.imgUrl,
            id: onePaper.id,
            netnode: onePaper.netnode,
        });
    } else {
        clusterFourPaper.value += 1;
    }
    return Rep22s;
}, []);

//@ts-ignore
const Rep23s = ([].concat.apply([], Rep23Data) as SankeyLink[]).reduce<SankeyLinkExtended[]>((Rep23s, onePaper) => {
    //@ts-ignore
    const clusterFourPaper = Rep23s.find(
        (r) => r.source === onePaper.source && r.target === onePaper.target && r.value === onePaper.value && r.valueid === onePaper.valueid && r.process === onePaper.process
    );

    if (!clusterFourPaper) {
        //@ts-ignore
        Rep23s.push({
            source: onePaper.source,
            target: onePaper.target,
            value: onePaper.value,
            valueid: onePaper.valueid,
            paperName: onePaper.paperName,
            process: onePaper.process,
            imgUrl: onePaper.imgUrl,
            id: onePaper.id,
            netnode: onePaper.netnode,
        });
    } else {
        clusterFourPaper.value += 1;
    }
    return Rep23s;
}, []);

//@ts-ignore
const Rep24s = ([].concat.apply([], Rep24Data) as SankeyLink[]).reduce<SankeyLinkExtended[]>((Rep24s, onePaper) => {
    //@ts-ignore
    const clusterFourPaper = Rep24s.find(
        (r) => r.source === onePaper.source && r.target === onePaper.target && r.value === onePaper.value && r.valueid === onePaper.valueid && r.process === onePaper.process
    );

    if (!clusterFourPaper) {
        //@ts-ignore
        Rep24s.push({
            source: onePaper.source,
            target: onePaper.target,
            value: onePaper.value,
            valueid: onePaper.valueid,
            paperName: onePaper.paperName,
            process: onePaper.process,
            imgUrl: onePaper.imgUrl,
            id: onePaper.id,
            netnode: onePaper.netnode,
        });
    } else {
        clusterFourPaper.value += 1;
    }
    return Rep24s;
}, []);

//@ts-ignore
const Var1s = ([].concat.apply([], Var1Data) as SankeyLink[]).reduce<SankeyLinkExtended[]>((Var1s, onePaper) => {
    //@ts-ignore
    const clusterFourPaper = Var1s.find(
        (r) => r.source === onePaper.source && r.target === onePaper.target && r.value === onePaper.value && r.valueid === onePaper.valueid && r.process === onePaper.process
    );

    if (!clusterFourPaper) {
        //@ts-ignore
        Var1s.push({
            source: onePaper.source,
            target: onePaper.target,
            value: onePaper.value,
            valueid: onePaper.valueid,
            paperName: onePaper.paperName,
            process: onePaper.process,
            imgUrl: onePaper.imgUrl,
            id: onePaper.id,
            netnode: onePaper.netnode,
        });
    } else {
        clusterFourPaper.value += 1;
    }
    return Var1s;
}, []);

//@ts-ignore
const Var2s = ([].concat.apply([], Var2Data) as SankeyLink[]).reduce<SankeyLinkExtended[]>((Var2s, onePaper) => {
    //@ts-ignore
    const clusterFourPaper = Var2s.find(
        (r) => r.source === onePaper.source && r.target === onePaper.target && r.value === onePaper.value && r.valueid === onePaper.valueid && r.process === onePaper.process
    );

    if (!clusterFourPaper) {
        //@ts-ignore
        Var2s.push({
            source: onePaper.source,
            target: onePaper.target,
            value: onePaper.value,
            valueid: onePaper.valueid,
            paperName: onePaper.paperName,
            process: onePaper.process,
            imgUrl: onePaper.imgUrl,
            id: onePaper.id,
            netnode: onePaper.netnode,
        });
    } else {
        clusterFourPaper.value += 1;
    }
    return Var2s;
}, []);

//@ts-ignore
const Var3s = ([].concat.apply([], Var3Data) as SankeyLink[]).reduce<SankeyLinkExtended[]>((Var3s, onePaper) => {
    //@ts-ignore
    const clusterFourPaper = Var1s.find(
        (r) => r.source === onePaper.source && r.target === onePaper.target && r.value === onePaper.value && r.valueid === onePaper.valueid && r.process === onePaper.process
    );

    if (!clusterFourPaper) {
        //@ts-ignore
        Var3s.push({
            source: onePaper.source,
            target: onePaper.target,
            value: onePaper.value,
            valueid: onePaper.valueid,
            paperName: onePaper.paperName,
            process: onePaper.process,
            imgUrl: onePaper.imgUrl,
            id: onePaper.id,
            netnode: onePaper.netnode,
        });
    } else {
        clusterFourPaper.value += 1;
    }
    return Var3s;
}, []);

//@ts-ignore
const Var4s = ([].concat.apply([], Var4Data) as SankeyLink[]).reduce<SankeyLinkExtended[]>((Var4s, onePaper) => {
    //@ts-ignore
    const clusterFourPaper = Var4s.find(
        (r) => r.source === onePaper.source && r.target === onePaper.target && r.value === onePaper.value && r.valueid === onePaper.valueid && r.process === onePaper.process
    );

    if (!clusterFourPaper) {
        //@ts-ignore
        Var4s.push({
            source: onePaper.source,
            target: onePaper.target,
            value: onePaper.value,
            valueid: onePaper.valueid,
            paperName: onePaper.paperName,
            process: onePaper.process,
            imgUrl: onePaper.imgUrl,
            id: onePaper.id,
            netnode: onePaper.netnode,
        });
    } else {
        clusterFourPaper.value += 1;
    }
    return Var4s;
}, []);

//@ts-ignore
const Var5s = ([].concat.apply([], Var5Data) as SankeyLink[]).reduce<SankeyLinkExtended[]>((Var5s, onePaper) => {
    //@ts-ignore
    const clusterFourPaper = Var5s.find(
        (r) => r.source === onePaper.source && r.target === onePaper.target && r.value === onePaper.value && r.valueid === onePaper.valueid && r.process === onePaper.process
    );

    if (!clusterFourPaper) {
        //@ts-ignore
        Var5s.push({
            source: onePaper.source,
            target: onePaper.target,
            value: onePaper.value,
            valueid: onePaper.valueid,
            paperName: onePaper.paperName,
            process: onePaper.process,
            imgUrl: onePaper.imgUrl,
            id: onePaper.id,
            netnode: onePaper.netnode,
        });
    } else {
        clusterFourPaper.value += 1;
    }
    return Var5s;
}, []);

//@ts-ignore
const Var6s = ([].concat.apply([], Var6Data) as SankeyLink[]).reduce<SankeyLinkExtended[]>((Var6s, onePaper) => {
    //@ts-ignore
    const clusterFourPaper = Var6s.find(
        (r) => r.source === onePaper.source && r.target === onePaper.target && r.value === onePaper.value && r.valueid === onePaper.valueid && r.process === onePaper.process
    );

    if (!clusterFourPaper) {
        //@ts-ignore
        Var6s.push({
            source: onePaper.source,
            target: onePaper.target,
            value: onePaper.value,
            valueid: onePaper.valueid,
            paperName: onePaper.paperName,
            process: onePaper.process,
            imgUrl: onePaper.imgUrl,
            id: onePaper.id,
            netnode: onePaper.netnode,
        });
    } else {
        clusterFourPaper.value += 1;
    }
    return Var6s;
}, []);

//@ts-ignore
const Var7s = ([].concat.apply([], Var7Data) as SankeyLink[]).reduce<SankeyLinkExtended[]>((Var7s, onePaper) => {
    //@ts-ignore
    const clusterFourPaper = Var7s.find(
        (r) => r.source === onePaper.source && r.target === onePaper.target && r.value === onePaper.value && r.valueid === onePaper.valueid && r.process === onePaper.process
    );

    if (!clusterFourPaper) {
        //@ts-ignore
        Var7s.push({
            source: onePaper.source,
            target: onePaper.target,
            value: onePaper.value,
            valueid: onePaper.valueid,
            paperName: onePaper.paperName,
            process: onePaper.process,
            imgUrl: onePaper.imgUrl,
            id: onePaper.id,
            netnode: onePaper.netnode,
        });
    } else {
        clusterFourPaper.value += 1;
    }
    return Var7s;
}, []);

//@ts-ignore
const Tec1s = ([].concat.apply([], Tec1Data) as SankeyLink[]).reduce<SankeyLinkExtended[]>((Tec1s, onePaper) => {
    //@ts-ignore
    const clusterFourPaper = Tec1s.find(
        (r) => r.source === onePaper.source && r.target === onePaper.target && r.value === onePaper.value && r.valueid === onePaper.valueid && r.process === onePaper.process
    );

    if (!clusterFourPaper) {
        //@ts-ignore
        Tec1s.push({
            source: onePaper.source,
            target: onePaper.target,
            value: onePaper.value,
            valueid: onePaper.valueid,
            paperName: onePaper.paperName,
            process: onePaper.process,
            imgUrl: onePaper.imgUrl,
            id: onePaper.id,
            netnode: onePaper.netnode,
        });
    } else {
        clusterFourPaper.value += 1;
    }
    return Tec1s;
}, []);

//@ts-ignore
const Tec2s = ([].concat.apply([], Tec2Data) as SankeyLink[]).reduce<SankeyLinkExtended[]>((Tec2s, onePaper) => {
    //@ts-ignore
    const clusterFourPaper = Tec2s.find(
        (r) => r.source === onePaper.source && r.target === onePaper.target && r.value === onePaper.value && r.valueid === onePaper.valueid && r.process === onePaper.process
    );

    if (!clusterFourPaper) {
        //@ts-ignore
        Tec2s.push({
            source: onePaper.source,
            target: onePaper.target,
            value: onePaper.value,
            valueid: onePaper.valueid,
            paperName: onePaper.paperName,
            process: onePaper.process,
            imgUrl: onePaper.imgUrl,
            id: onePaper.id,
            netnode: onePaper.netnode,
        });
    } else {
        clusterFourPaper.value += 1;
    }
    return Tec2s;
}, []);

//@ts-ignore
const Tec3s = ([].concat.apply([], Tec3Data) as SankeyLink[]).reduce<SankeyLinkExtended[]>((Tec3s, onePaper) => {
    //@ts-ignore
    const clusterFourPaper = Tec3s.find(
        (r) => r.source === onePaper.source && r.target === onePaper.target && r.value === onePaper.value && r.valueid === onePaper.valueid && r.process === onePaper.process
    );

    if (!clusterFourPaper) {
        //@ts-ignore
        Tec3s.push({
            source: onePaper.source,
            target: onePaper.target,
            value: onePaper.value,
            valueid: onePaper.valueid,
            paperName: onePaper.paperName,
            process: onePaper.process,
            imgUrl: onePaper.imgUrl,
            id: onePaper.id,
            netnode: onePaper.netnode,
        });
    } else {
        clusterFourPaper.value += 1;
    }
    return Tec3s;
}, []);

//@ts-ignore
const Tec4s = ([].concat.apply([], Tec4Data) as SankeyLink[]).reduce<SankeyLinkExtended[]>((Tec4s, onePaper) => {
    //@ts-ignore
    const clusterFourPaper = Tec4s.find(
        (r) => r.source === onePaper.source && r.target === onePaper.target && r.value === onePaper.value && r.valueid === onePaper.valueid && r.process === onePaper.process
    );

    if (!clusterFourPaper) {
        //@ts-ignore
        Tec4s.push({
            source: onePaper.source,
            target: onePaper.target,
            value: onePaper.value,
            valueid: onePaper.valueid,
            paperName: onePaper.paperName,
            process: onePaper.process,
            imgUrl: onePaper.imgUrl,
            id: onePaper.id,
            netnode: onePaper.netnode,
        });
    } else {
        clusterFourPaper.value += 1;
    }
    return Tec4s;
}, []);

//@ts-ignore
const Tec5s = ([].concat.apply([], Tec5Data) as SankeyLink[]).reduce<SankeyLinkExtended[]>((Tec5s, onePaper) => {
    //@ts-ignore
    const clusterFourPaper = Tec5s.find(
        (r) => r.source === onePaper.source && r.target === onePaper.target && r.value === onePaper.value && r.valueid === onePaper.valueid && r.process === onePaper.process
    );

    if (!clusterFourPaper) {
        //@ts-ignore
        Tec5s.push({
            source: onePaper.source,
            target: onePaper.target,
            value: onePaper.value,
            valueid: onePaper.valueid,
            paperName: onePaper.paperName,
            process: onePaper.process,
            imgUrl: onePaper.imgUrl,
            id: onePaper.id,
            netnode: onePaper.netnode,
        });
    } else {
        clusterFourPaper.value += 1;
    }
    return Tec5s;
}, []);

//@ts-ignore
const Tec6s = ([].concat.apply([], Tec6Data) as SankeyLink[]).reduce<SankeyLinkExtended[]>((Tec6s, onePaper) => {
    //@ts-ignore
    const clusterFourPaper = Tec6s.find(
        (r) => r.source === onePaper.source && r.target === onePaper.target && r.value === onePaper.value && r.valueid === onePaper.valueid && r.process === onePaper.process
    );

    if (!clusterFourPaper) {
        //@ts-ignore
        Tec6s.push({
            source: onePaper.source,
            target: onePaper.target,
            value: onePaper.value,
            valueid: onePaper.valueid,
            paperName: onePaper.paperName,
            process: onePaper.process,
            imgUrl: onePaper.imgUrl,
            id: onePaper.id,
            netnode: onePaper.netnode,
        });
    } else {
        clusterFourPaper.value += 1;
    }
    return Tec6s;
}, []);

//@ts-ignore
const Tec7s = ([].concat.apply([], Tec7Data) as SankeyLink[]).reduce<SankeyLinkExtended[]>((Tec7s, onePaper) => {
    //@ts-ignore
    const clusterFourPaper = Tec7s.find(
        (r) => r.source === onePaper.source && r.target === onePaper.target && r.value === onePaper.value && r.valueid === onePaper.valueid && r.process === onePaper.process
    );

    if (!clusterFourPaper) {
        //@ts-ignore
        Tec7s.push({
            source: onePaper.source,
            target: onePaper.target,
            value: onePaper.value,
            valueid: onePaper.valueid,
            paperName: onePaper.paperName,
            process: onePaper.process,
            imgUrl: onePaper.imgUrl,
            id: onePaper.id,
            netnode: onePaper.netnode,
        });
    } else {
        clusterFourPaper.value += 1;
    }
    return Tec7s;
}, []);

//@ts-ignore
const Tec8s = ([].concat.apply([], Tec8Data) as SankeyLink[]).reduce<SankeyLinkExtended[]>((Tec8s, onePaper) => {
    //@ts-ignore
    const clusterFourPaper = Tec8s.find(
        (r) => r.source === onePaper.source && r.target === onePaper.target && r.value === onePaper.value && r.valueid === onePaper.valueid && r.process === onePaper.process
    );

    if (!clusterFourPaper) {
        //@ts-ignore
        Tec8s.push({
            source: onePaper.source,
            target: onePaper.target,
            value: onePaper.value,
            valueid: onePaper.valueid,
            paperName: onePaper.paperName,
            process: onePaper.process,
            imgUrl: onePaper.imgUrl,
            id: onePaper.id,
            netnode: onePaper.netnode,
        });
    } else {
        clusterFourPaper.value += 1;
    }
    return Tec8s;
}, []);

//@ts-ignore
const Tec9s = ([].concat.apply([], Tec9Data) as SankeyLink[]).reduce<SankeyLinkExtended[]>((Tec9s, onePaper) => {
    //@ts-ignore
    const clusterFourPaper = Tec9s.find(
        (r) => r.source === onePaper.source && r.target === onePaper.target && r.value === onePaper.value && r.valueid === onePaper.valueid && r.process === onePaper.process
    );

    if (!clusterFourPaper) {
        //@ts-ignore
        Tec9s.push({
            source: onePaper.source,
            target: onePaper.target,
            value: onePaper.value,
            valueid: onePaper.valueid,
            paperName: onePaper.paperName,
            process: onePaper.process,
            imgUrl: onePaper.imgUrl,
            id: onePaper.id,
            netnode: onePaper.netnode,
        });
    } else {
        clusterFourPaper.value += 1;
    }
    return Tec9s;
}, []);

//@ts-ignore
const Tec10s = ([].concat.apply([], Tec10Data) as SankeyLink[]).reduce<SankeyLinkExtended[]>((Tec10s, onePaper) => {
    //@ts-ignore
    const clusterFourPaper = Tec10s.find(
        (r) => r.source === onePaper.source && r.target === onePaper.target && r.value === onePaper.value && r.valueid === onePaper.valueid && r.process === onePaper.process
    );

    if (!clusterFourPaper) {
        //@ts-ignore
        Tec10s.push({
            source: onePaper.source,
            target: onePaper.target,
            value: onePaper.value,
            valueid: onePaper.valueid,
            paperName: onePaper.paperName,
            process: onePaper.process,
            imgUrl: onePaper.imgUrl,
            id: onePaper.id,
            netnode: onePaper.netnode,
        });
    } else {
        clusterFourPaper.value += 1;
    }
    return Tec10s;
}, []);

//@ts-ignore
const Tec11s = ([].concat.apply([], Tec11Data) as SankeyLink[]).reduce<SankeyLinkExtended[]>((Tec11s, onePaper) => {
    //@ts-ignore
    const clusterFourPaper = Tec11s.find(
        (r) => r.source === onePaper.source && r.target === onePaper.target && r.value === onePaper.value && r.valueid === onePaper.valueid && r.process === onePaper.process
    );

    if (!clusterFourPaper) {
        //@ts-ignore
        Tec11s.push({
            source: onePaper.source,
            target: onePaper.target,
            value: onePaper.value,
            valueid: onePaper.valueid,
            paperName: onePaper.paperName,
            process: onePaper.process,
            imgUrl: onePaper.imgUrl,
            id: onePaper.id,
            netnode: onePaper.netnode,
        });
    } else {
        clusterFourPaper.value += 1;
    }
    return Tec11s;
}, []);

//@ts-ignore
const Tec12s = ([].concat.apply([], Tec12Data) as SankeyLink[]).reduce<SankeyLinkExtended[]>((Tec12s, onePaper) => {
    //@ts-ignore
    const clusterFourPaper = Tec12s.find(
        (r) => r.source === onePaper.source && r.target === onePaper.target && r.value === onePaper.value && r.valueid === onePaper.valueid && r.process === onePaper.process
    );

    if (!clusterFourPaper) {
        //@ts-ignore
        Tec12s.push({
            source: onePaper.source,
            target: onePaper.target,
            value: onePaper.value,
            valueid: onePaper.valueid,
            paperName: onePaper.paperName,
            process: onePaper.process,
            imgUrl: onePaper.imgUrl,
            id: onePaper.id,
            netnode: onePaper.netnode,
        });
    } else {
        clusterFourPaper.value += 1;
    }
    return Tec12s;
}, []);

//@ts-ignore
const Tec13s = ([].concat.apply([], Tec13Data) as SankeyLink[]).reduce<SankeyLinkExtended[]>((Tec13s, onePaper) => {
    //@ts-ignore
    const clusterFourPaper = Tec13s.find(
        (r) => r.source === onePaper.source && r.target === onePaper.target && r.value === onePaper.value && r.valueid === onePaper.valueid && r.process === onePaper.process
    );

    if (!clusterFourPaper) {
        //@ts-ignore
        Tec13s.push({
            source: onePaper.source,
            target: onePaper.target,
            value: onePaper.value,
            valueid: onePaper.valueid,
            paperName: onePaper.paperName,
            process: onePaper.process,
            imgUrl: onePaper.imgUrl,
            id: onePaper.id,
            netnode: onePaper.netnode,
        });
    } else {
        clusterFourPaper.value += 1;
    }
    return Tec13s;
}, []);

//@ts-ignore
const Tec14s = ([].concat.apply([], Tec14Data) as SankeyLink[]).reduce<SankeyLinkExtended[]>((Tec14s, onePaper) => {
    //@ts-ignore
    const clusterFourPaper = Tec14s.find(
        (r) => r.source === onePaper.source && r.target === onePaper.target && r.value === onePaper.value && r.valueid === onePaper.valueid && r.process === onePaper.process
    );

    if (!clusterFourPaper) {
        //@ts-ignore
        Tec14s.push({
            source: onePaper.source,
            target: onePaper.target,
            value: onePaper.value,
            valueid: onePaper.valueid,
            paperName: onePaper.paperName,
            process: onePaper.process,
            imgUrl: onePaper.imgUrl,
            id: onePaper.id,
            netnode: onePaper.netnode,
        });
    } else {
        clusterFourPaper.value += 1;
    }
    return Tec14s;
}, []);

//@ts-ignore
const Tec15s = ([].concat.apply([], Tec15Data) as SankeyLink[]).reduce<SankeyLinkExtended[]>((Tec15s, onePaper) => {
    //@ts-ignore
    const clusterFourPaper = Tec15s.find(
        (r) => r.source === onePaper.source && r.target === onePaper.target && r.value === onePaper.value && r.valueid === onePaper.valueid && r.process === onePaper.process
    );

    if (!clusterFourPaper) {
        //@ts-ignore
        Tec15s.push({
            source: onePaper.source,
            target: onePaper.target,
            value: onePaper.value,
            valueid: onePaper.valueid,
            paperName: onePaper.paperName,
            process: onePaper.process,
            imgUrl: onePaper.imgUrl,
            id: onePaper.id,
            netnode: onePaper.netnode,
        });
    } else {
        clusterFourPaper.value += 1;
    }
    return Tec15s;
}, []);

const statusImgSet = PaperStatus.reduce(function (acc, cur) {
    return acc.concat(cur);
});

//@ts-ignore
const Tar1s = ([].concat.apply([], Tar1Data) as SankeyLink[]).reduce<SankeyLinkExtended[]>((Tar1s, onePaper) => {
    //@ts-ignore
    const clusterFourPaper = Tar1s.find(
        (r) => r.source === onePaper.source && r.target === onePaper.target && r.value === onePaper.value && r.valueid === onePaper.valueid && r.process === onePaper.process
    );

    if (!clusterFourPaper) {
        //@ts-ignore
        Tar1s.push({
            source: onePaper.source,
            target: onePaper.target,
            value: onePaper.value,
            valueid: onePaper.valueid,
            paperName: onePaper.paperName,
            process: onePaper.process,
            imgUrl: onePaper.imgUrl,
            id: onePaper.id,
            netnode: onePaper.netnode,
        });
    } else {
        clusterFourPaper.value += 1;
    }
    return Tar1s;
}, []);

//@ts-ignore
const Tar2s = ([].concat.apply([], Tar2Data) as SankeyLink[]).reduce<SankeyLinkExtended[]>((Tar2s, onePaper) => {
    //@ts-ignore
    const clusterFourPaper = Tar2s.find(
        (r) => r.source === onePaper.source && r.target === onePaper.target && r.value === onePaper.value && r.valueid === onePaper.valueid && r.process === onePaper.process
    );

    if (!clusterFourPaper) {
        //@ts-ignore
        Tar2s.push({
            source: onePaper.source,
            target: onePaper.target,
            value: onePaper.value,
            valueid: onePaper.valueid,
            paperName: onePaper.paperName,
            process: onePaper.process,
            imgUrl: onePaper.imgUrl,
            id: onePaper.id,
            netnode: onePaper.netnode,
        });
    } else {
        clusterFourPaper.value += 1;
    }
    return Tar2s;
}, []);

//@ts-ignore
const Tar3s = ([].concat.apply([], Tar3Data) as SankeyLink[]).reduce<SankeyLinkExtended[]>((Tar3s, onePaper) => {
    //@ts-ignore
    const clusterFourPaper = Tar3s.find(
        (r) => r.source === onePaper.source && r.target === onePaper.target && r.value === onePaper.value && r.valueid === onePaper.valueid && r.process === onePaper.process
    );

    if (!clusterFourPaper) {
        //@ts-ignore
        Tar3s.push({
            source: onePaper.source,
            target: onePaper.target,
            value: onePaper.value,
            valueid: onePaper.valueid,
            paperName: onePaper.paperName,
            process: onePaper.process,
            imgUrl: onePaper.imgUrl,
            id: onePaper.id,
            netnode: onePaper.netnode,
        });
    } else {
        clusterFourPaper.value += 1;
    }
    return Tar3s;
}, []);

//@ts-ignore
const Tar4s = ([].concat.apply([], Tar4Data) as SankeyLink[]).reduce<SankeyLinkExtended[]>((Tar4s, onePaper) => {
    //@ts-ignore
    const clusterFourPaper = Tar4s.find(
        (r) => r.source === onePaper.source && r.target === onePaper.target && r.value === onePaper.value && r.valueid === onePaper.valueid && r.process === onePaper.process
    );

    if (!clusterFourPaper) {
        //@ts-ignore
        Tar4s.push({
            source: onePaper.source,
            target: onePaper.target,
            value: onePaper.value,
            valueid: onePaper.valueid,
            paperName: onePaper.paperName,
            process: onePaper.process,
            imgUrl: onePaper.imgUrl,
            id: onePaper.id,
            netnode: onePaper.netnode,
        });
    } else {
        clusterFourPaper.value += 1;
    }
    return Tar4s;
}, []);

//@ts-ignore
const Tar5s = ([].concat.apply([], Tar5Data) as SankeyLink[]).reduce<SankeyLinkExtended[]>((Tar5s, onePaper) => {
    //@ts-ignore
    const clusterFourPaper = Tar5s.find(
        (r) => r.source === onePaper.source && r.target === onePaper.target && r.value === onePaper.value && r.valueid === onePaper.valueid && r.process === onePaper.process
    );

    if (!clusterFourPaper) {
        //@ts-ignore
        Tar5s.push({
            source: onePaper.source,
            target: onePaper.target,
            value: onePaper.value,
            valueid: onePaper.valueid,
            paperName: onePaper.paperName,
            process: onePaper.process,
            imgUrl: onePaper.imgUrl,
            id: onePaper.id,
            netnode: onePaper.netnode,
        });
    } else {
        clusterFourPaper.value += 1;
    }
    return Tar5s;
}, []);

//@ts-ignore
const Tar6s = ([].concat.apply([], Tar6Data) as SankeyLink[]).reduce<SankeyLinkExtended[]>((Tar6s, onePaper) => {
    //@ts-ignore
    const clusterFourPaper = Tar6s.find(
        (r) => r.source === onePaper.source && r.target === onePaper.target && r.value === onePaper.value && r.valueid === onePaper.valueid && r.process === onePaper.process
    );

    if (!clusterFourPaper) {
        //@ts-ignore
        Tar6s.push({
            source: onePaper.source,
            target: onePaper.target,
            value: onePaper.value,
            valueid: onePaper.valueid,
            paperName: onePaper.paperName,
            process: onePaper.process,
            imgUrl: onePaper.imgUrl,
            id: onePaper.id,
            netnode: onePaper.netnode,
        });
    } else {
        clusterFourPaper.value += 1;
    }
    return Tar6s;
}, []);

//@ts-ignore
const Tar7s = ([].concat.apply([], Tar7Data) as SankeyLink[]).reduce<SankeyLinkExtended[]>((Tar7s, onePaper) => {
    //@ts-ignore
    const clusterFourPaper = Tar7s.find(
        (r) => r.source === onePaper.source && r.target === onePaper.target && r.value === onePaper.value && r.valueid === onePaper.valueid && r.process === onePaper.process
    );

    if (!clusterFourPaper) {
        //@ts-ignore
        Tar7s.push({
            source: onePaper.source,
            target: onePaper.target,
            value: onePaper.value,
            valueid: onePaper.valueid,
            paperName: onePaper.paperName,
            process: onePaper.process,
            imgUrl: onePaper.imgUrl,
            id: onePaper.id,
            netnode: onePaper.netnode,
        });
    } else {
        clusterFourPaper.value += 1;
    }
    return Tar7s;
}, []);

//@ts-ignore
const Tar8s = ([].concat.apply([], Tar8Data) as SankeyLink[]).reduce<SankeyLinkExtended[]>((Tar8s, onePaper) => {
    //@ts-ignore
    const clusterFourPaper = Tar8s.find(
        (r) => r.source === onePaper.source && r.target === onePaper.target && r.value === onePaper.value && r.valueid === onePaper.valueid && r.process === onePaper.process
    );

    if (!clusterFourPaper) {
        //@ts-ignore
        Tar8s.push({
            source: onePaper.source,
            target: onePaper.target,
            value: onePaper.value,
            valueid: onePaper.valueid,
            paperName: onePaper.paperName,
            process: onePaper.process,
            imgUrl: onePaper.imgUrl,
            id: onePaper.id,
            netnode: onePaper.netnode,
        });
    } else {
        clusterFourPaper.value += 1;
    }
    return Tar8s;
}, []);

//@ts-ignore
const Tar9s = ([].concat.apply([], Tar9Data) as SankeyLink[]).reduce<SankeyLinkExtended[]>((Tar9s, onePaper) => {
    //@ts-ignore
    const clusterFourPaper = Tar9s.find(
        (r) => r.source === onePaper.source && r.target === onePaper.target && r.value === onePaper.value && r.valueid === onePaper.valueid && r.process === onePaper.process
    );

    if (!clusterFourPaper) {
        //@ts-ignore
        Tar9s.push({
            source: onePaper.source,
            target: onePaper.target,
            value: onePaper.value,
            valueid: onePaper.valueid,
            paperName: onePaper.paperName,
            process: onePaper.process,
            imgUrl: onePaper.imgUrl,
            id: onePaper.id,
            netnode: onePaper.netnode,
        });
    } else {
        clusterFourPaper.value += 1;
    }
    return Tar9s;
}, []);

//@ts-ignore
const Tar10s = ([].concat.apply([], Tar10Data) as SankeyLink[]).reduce<SankeyLinkExtended[]>((Tar10s, onePaper) => {
    //@ts-ignore
    const clusterFourPaper = Tar10s.find(
        (r) => r.source === onePaper.source && r.target === onePaper.target && r.value === onePaper.value && r.valueid === onePaper.valueid && r.process === onePaper.process
    );

    if (!clusterFourPaper) {
        //@ts-ignore
        Tar10s.push({
            source: onePaper.source,
            target: onePaper.target,
            value: onePaper.value,
            valueid: onePaper.valueid,
            paperName: onePaper.paperName,
            process: onePaper.process,
            imgUrl: onePaper.imgUrl,
            id: onePaper.id,
            netnode: onePaper.netnode,
        });
    } else {
        clusterFourPaper.value += 1;
    }
    return Tar10s;
}, []);

//@ts-ignore
const Tar11s = ([].concat.apply([], Tar11Data) as SankeyLink[]).reduce<SankeyLinkExtended[]>((Tar11s, onePaper) => {
    //@ts-ignore
    const clusterFourPaper = Tar11s.find(
        (r) => r.source === onePaper.source && r.target === onePaper.target && r.value === onePaper.value && r.valueid === onePaper.valueid && r.process === onePaper.process
    );

    if (!clusterFourPaper) {
        //@ts-ignore
        Tar11s.push({
            source: onePaper.source,
            target: onePaper.target,
            value: onePaper.value,
            valueid: onePaper.valueid,
            paperName: onePaper.paperName,
            process: onePaper.process,
            imgUrl: onePaper.imgUrl,
            id: onePaper.id,
            netnode: onePaper.netnode,
        });
    } else {
        clusterFourPaper.value += 1;
    }
    return Tar11s;
}, []);

//@ts-ignore
const Tar12s = ([].concat.apply([], Tar12Data) as SankeyLink[]).reduce<SankeyLinkExtended[]>((Tar12s, onePaper) => {
    //@ts-ignore
    const clusterFourPaper = Tar12s.find(
        (r) => r.source === onePaper.source && r.target === onePaper.target && r.value === onePaper.value && r.valueid === onePaper.valueid && r.process === onePaper.process
    );

    if (!clusterFourPaper) {
        //@ts-ignore
        Tar12s.push({
            source: onePaper.source,
            target: onePaper.target,
            value: onePaper.value,
            valueid: onePaper.valueid,
            paperName: onePaper.paperName,
            process: onePaper.process,
            imgUrl: onePaper.imgUrl,
            id: onePaper.id,
            netnode: onePaper.netnode,
        });
    } else {
        clusterFourPaper.value += 1;
    }
    return Tar12s;
}, []);

//@ts-ignore
const Tar13s = ([].concat.apply([], Tar13Data) as SankeyLink[]).reduce<SankeyLinkExtended[]>((Tar13s, onePaper) => {
    //@ts-ignore
    const clusterFourPaper = Tar13s.find(
        (r) => r.source === onePaper.source && r.target === onePaper.target && r.value === onePaper.value && r.valueid === onePaper.valueid && r.process === onePaper.process
    );

    if (!clusterFourPaper) {
        //@ts-ignore
        Tar13s.push({
            source: onePaper.source,
            target: onePaper.target,
            value: onePaper.value,
            valueid: onePaper.valueid,
            paperName: onePaper.paperName,
            process: onePaper.process,
            imgUrl: onePaper.imgUrl,
            id: onePaper.id,
            netnode: onePaper.netnode,
        });
    } else {
        clusterFourPaper.value += 1;
    }
    return Tar13s;
}, []);

//@ts-ignore
const Tar14s = ([].concat.apply([], Tar14Data) as SankeyLink[]).reduce<SankeyLinkExtended[]>((Tar14s, onePaper) => {
    //@ts-ignore
    const clusterFourPaper = Tar14s.find(
        (r) => r.source === onePaper.source && r.target === onePaper.target && r.value === onePaper.value && r.valueid === onePaper.valueid && r.process === onePaper.process
    );

    if (!clusterFourPaper) {
        //@ts-ignore
        Tar14s.push({
            source: onePaper.source,
            target: onePaper.target,
            value: onePaper.value,
            valueid: onePaper.valueid,
            paperName: onePaper.paperName,
            process: onePaper.process,
            imgUrl: onePaper.imgUrl,
            id: onePaper.id,
            netnode: onePaper.netnode,
        });
    } else {
        clusterFourPaper.value += 1;
    }
    return Tar14s;
}, []);

//@ts-ignore
const Tar15s = ([].concat.apply([], Tar15Data) as SankeyLink[]).reduce<SankeyLinkExtended[]>((Tar15s, onePaper) => {
    //@ts-ignore
    const clusterFourPaper = Tar15s.find(
        (r) => r.source === onePaper.source && r.target === onePaper.target && r.value === onePaper.value && r.valueid === onePaper.valueid && r.process === onePaper.process
    );

    if (!clusterFourPaper) {
        //@ts-ignore
        Tar15s.push({
            source: onePaper.source,
            target: onePaper.target,
            value: onePaper.value,
            valueid: onePaper.valueid,
            paperName: onePaper.paperName,
            process: onePaper.process,
            imgUrl: onePaper.imgUrl,
            id: onePaper.id,
            netnode: onePaper.netnode,
        });
    } else {
        clusterFourPaper.value += 1;
    }
    return Tar15s;
}, []);

//@ts-ignore
const Tar16s = ([].concat.apply([], Tar16Data) as SankeyLink[]).reduce<SankeyLinkExtended[]>((Tar16s, onePaper) => {
    //@ts-ignore
    const clusterFourPaper = Tar16s.find(
        (r) => r.source === onePaper.source && r.target === onePaper.target && r.value === onePaper.value && r.valueid === onePaper.valueid && r.process === onePaper.process
    );

    if (!clusterFourPaper) {
        //@ts-ignore
        Tar16s.push({
            source: onePaper.source,
            target: onePaper.target,
            value: onePaper.value,
            valueid: onePaper.valueid,
            paperName: onePaper.paperName,
            process: onePaper.process,
            imgUrl: onePaper.imgUrl,
            id: onePaper.id,
            netnode: onePaper.netnode,
        });
    } else {
        clusterFourPaper.value += 1;
    }
    return Tar16s;
}, []);

//@ts-ignore
const Tar17s = ([].concat.apply([], Tar17Data) as SankeyLink[]).reduce<SankeyLinkExtended[]>((Tar17s, onePaper) => {
    //@ts-ignore
    const clusterFourPaper = Tar17s.find(
        (r) => r.source === onePaper.source && r.target === onePaper.target && r.value === onePaper.value && r.valueid === onePaper.valueid && r.process === onePaper.process
    );

    if (!clusterFourPaper) {
        //@ts-ignore
        Tar17s.push({
            source: onePaper.source,
            target: onePaper.target,
            value: onePaper.value,
            valueid: onePaper.valueid,
            paperName: onePaper.paperName,
            process: onePaper.process,
            imgUrl: onePaper.imgUrl,
            id: onePaper.id,
            netnode: onePaper.netnode,
        });
    } else {
        clusterFourPaper.value += 1;
    }
    return Tar17s;
}, []);

//@ts-ignore
const Tar18s = ([].concat.apply([], Tar18Data) as SankeyLink[]).reduce<SankeyLinkExtended[]>((Tar18s, onePaper) => {
    //@ts-ignore
    const clusterFourPaper = Tar18s.find(
        (r) => r.source === onePaper.source && r.target === onePaper.target && r.value === onePaper.value && r.valueid === onePaper.valueid && r.process === onePaper.process
    );

    if (!clusterFourPaper) {
        //@ts-ignore
        Tar18s.push({
            source: onePaper.source,
            target: onePaper.target,
            value: onePaper.value,
            valueid: onePaper.valueid,
            paperName: onePaper.paperName,
            process: onePaper.process,
            imgUrl: onePaper.imgUrl,
            id: onePaper.id,
            netnode: onePaper.netnode,
        });
    } else {
        clusterFourPaper.value += 1;
    }
    return Tar18s;
}, []);

//@ts-ignore
const Tar19s = ([].concat.apply([], Tar19Data) as SankeyLink[]).reduce<SankeyLinkExtended[]>((Tar19s, onePaper) => {
    //@ts-ignore
    const clusterFourPaper = Tar19s.find(
        (r) => r.source === onePaper.source && r.target === onePaper.target && r.value === onePaper.value && r.valueid === onePaper.valueid && r.process === onePaper.process
    );

    if (!clusterFourPaper) {
        //@ts-ignore
        Tar19s.push({
            source: onePaper.source,
            target: onePaper.target,
            value: onePaper.value,
            valueid: onePaper.valueid,
            paperName: onePaper.paperName,
            process: onePaper.process,
            imgUrl: onePaper.imgUrl,
            id: onePaper.id,
            netnode: onePaper.netnode,
        });
    } else {
        clusterFourPaper.value += 1;
    }
    return Tar19s;
}, []);

//@ts-ignore
const Tar20s = ([].concat.apply([], Tar20Data) as SankeyLink[]).reduce<SankeyLinkExtended[]>((Tar20s, onePaper) => {
    //@ts-ignore
    const clusterFourPaper = Tar20s.find(
        (r) => r.source === onePaper.source && r.target === onePaper.target && r.value === onePaper.value && r.valueid === onePaper.valueid && r.process === onePaper.process
    );

    if (!clusterFourPaper) {
        //@ts-ignore
        Tar20s.push({
            source: onePaper.source,
            target: onePaper.target,
            value: onePaper.value,
            valueid: onePaper.valueid,
            paperName: onePaper.paperName,
            process: onePaper.process,
            imgUrl: onePaper.imgUrl,
            id: onePaper.id,
            netnode: onePaper.netnode,
        });
    } else {
        clusterFourPaper.value += 1;
    }
    return Tar20s;
}, []);

//@ts-ignore
const Tar21s = ([].concat.apply([], Tar21Data) as SankeyLink[]).reduce<SankeyLinkExtended[]>((Tar21s, onePaper) => {
    //@ts-ignore
    const clusterFourPaper = Tar21s.find(
        (r) => r.source === onePaper.source && r.target === onePaper.target && r.value === onePaper.value && r.valueid === onePaper.valueid && r.process === onePaper.process
    );

    if (!clusterFourPaper) {
        //@ts-ignore
        Tar21s.push({
            source: onePaper.source,
            target: onePaper.target,
            value: onePaper.value,
            valueid: onePaper.valueid,
            paperName: onePaper.paperName,
            process: onePaper.process,
            imgUrl: onePaper.imgUrl,
            id: onePaper.id,
            netnode: onePaper.netnode,
        });
    } else {
        clusterFourPaper.value += 1;
    }
    return Tar21s;
}, []);

//@ts-ignore
const Tar22s = ([].concat.apply([], Tar22Data) as SankeyLink[]).reduce<SankeyLinkExtended[]>((Tar22s, onePaper) => {
    //@ts-ignore
    const clusterFourPaper = Tar22s.find(
        (r) => r.source === onePaper.source && r.target === onePaper.target && r.value === onePaper.value && r.valueid === onePaper.valueid && r.process === onePaper.process
    );

    if (!clusterFourPaper) {
        //@ts-ignore
        Tar22s.push({
            source: onePaper.source,
            target: onePaper.target,
            value: onePaper.value,
            valueid: onePaper.valueid,
            paperName: onePaper.paperName,
            process: onePaper.process,
            imgUrl: onePaper.imgUrl,
            id: onePaper.id,
            netnode: onePaper.netnode,
        });
    } else {
        clusterFourPaper.value += 1;
    }
    return Tar22s;
}, []);

//@ts-ignore
const Tar23s = ([].concat.apply([], Tar23Data) as SankeyLink[]).reduce<SankeyLinkExtended[]>((Tar23s, onePaper) => {
    //@ts-ignore
    const clusterFourPaper = Tar23s.find(
        (r) => r.source === onePaper.source && r.target === onePaper.target && r.value === onePaper.value && r.valueid === onePaper.valueid && r.process === onePaper.process
    );

    if (!clusterFourPaper) {
        //@ts-ignore
        Tar23s.push({
            source: onePaper.source,
            target: onePaper.target,
            value: onePaper.value,
            valueid: onePaper.valueid,
            paperName: onePaper.paperName,
            process: onePaper.process,
            imgUrl: onePaper.imgUrl,
            id: onePaper.id,
            netnode: onePaper.netnode,
        });
    } else {
        clusterFourPaper.value += 1;
    }
    return Tar23s;
}, []);

//@ts-ignore
const Tar24s = ([].concat.apply([], Tar24Data) as SankeyLink[]).reduce<SankeyLinkExtended[]>((Tar24s, onePaper) => {
    //@ts-ignore
    const clusterFourPaper = Tar24s.find(
        (r) => r.source === onePaper.source && r.target === onePaper.target && r.value === onePaper.value && r.valueid === onePaper.valueid && r.process === onePaper.process
    );

    if (!clusterFourPaper) {
        //@ts-ignore
        Tar24s.push({
            source: onePaper.source,
            target: onePaper.target,
            value: onePaper.value,
            valueid: onePaper.valueid,
            paperName: onePaper.paperName,
            process: onePaper.process,
            imgUrl: onePaper.imgUrl,
            id: onePaper.id,
            netnode: onePaper.netnode,
        });
    } else {
        clusterFourPaper.value += 1;
    }
    return Tar24s;
}, []);

//@ts-ignore
const Tar25s = ([].concat.apply([], Tar25Data) as SankeyLink[]).reduce<SankeyLinkExtended[]>((Tar25s, onePaper) => {
    //@ts-ignore
    const clusterFourPaper = Tar25s.find(
        (r) => r.source === onePaper.source && r.target === onePaper.target && r.value === onePaper.value && r.valueid === onePaper.valueid && r.process === onePaper.process
    );

    if (!clusterFourPaper) {
        //@ts-ignore
        Tar25s.push({
            source: onePaper.source,
            target: onePaper.target,
            value: onePaper.value,
            valueid: onePaper.valueid,
            paperName: onePaper.paperName,
            process: onePaper.process,
            imgUrl: onePaper.imgUrl,
            id: onePaper.id,
            netnode: onePaper.netnode,
        });
    } else {
        clusterFourPaper.value += 1;
    }
    return Tar25s;
}, []);

//@ts-ignore
const Tar26s = ([].concat.apply([], Tar26Data) as SankeyLink[]).reduce<SankeyLinkExtended[]>((Tar26s, onePaper) => {
    //@ts-ignore
    const clusterFourPaper = Tar26s.find(
        (r) => r.source === onePaper.source && r.target === onePaper.target && r.value === onePaper.value && r.valueid === onePaper.valueid && r.process === onePaper.process
    );

    if (!clusterFourPaper) {
        //@ts-ignore
        Tar26s.push({
            source: onePaper.source,
            target: onePaper.target,
            value: onePaper.value,
            valueid: onePaper.valueid,
            paperName: onePaper.paperName,
            process: onePaper.process,
            imgUrl: onePaper.imgUrl,
            id: onePaper.id,
            netnode: onePaper.netnode,
        });
    } else {
        clusterFourPaper.value += 1;
    }
    return Tar26s;
}, []);

//@ts-ignore
const Tar27s = ([].concat.apply([], Tar27Data) as SankeyLink[]).reduce<SankeyLinkExtended[]>((Tar27s, onePaper) => {
    //@ts-ignore
    const clusterFourPaper = Tar27s.find(
        (r) => r.source === onePaper.source && r.target === onePaper.target && r.value === onePaper.value && r.valueid === onePaper.valueid && r.process === onePaper.process
    );

    if (!clusterFourPaper) {
        //@ts-ignore
        Tar27s.push({
            source: onePaper.source,
            target: onePaper.target,
            value: onePaper.value,
            valueid: onePaper.valueid,
            paperName: onePaper.paperName,
            process: onePaper.process,
            imgUrl: onePaper.imgUrl,
            id: onePaper.id,
            netnode: onePaper.netnode,
        });
    } else {
        clusterFourPaper.value += 1;
    }
    return Tar27s;
}, []);

//@ts-ignore
const Node = CAA20.nodes;

export {
    Status,
    Node,
    Papers,
    PaperStatus,
    statusImgSet,
    AllPaperDatas,
    TargetAAs,
    TargetABs,
    TargetBAs,
    TargetBBs,
    TargetCAs,
    TargetCBs,
    RepAs,
    RepBs,
    RepCs,
    RepDs,
    RepEAs,
    RepEBs,
    RepFs,
    RepGs,
    RepHs,
    Emptys,
    ClusterOnes,
    ClusterTwos,
    ClusterThrees,
    ClusterFours,
    Int1s,
    Int2s,
    Int3s,
    Int4s,
    Int5s,
    Int6s,
    Int7s,
    Int8s,
    Int9s,
    Int10s,
    Int11s,
    Int12s,
    Int13s,
    Int14s,
    Int15s,
    Int16s,
    Rep1s,
    Rep2s,
    Rep3s,
    Rep4s,
    Rep5s,
    Rep6s,
    Rep7s,
    Rep8s,
    Rep9s,
    Rep10s,
    Rep11s,
    Rep12s,
    Rep13s,
    Rep14s,
    Rep15s,
    Rep16s,
    Rep17s,
    Rep18s,
    Rep19s,
    Rep20s,
    Rep21s,
    Rep22s,
    Rep23s,
    Rep24s,
    Var1s,
    Var2s,
    Var3s,
    Var4s,
    Var5s,
    Var6s,
    Var7s,
    Tec1s,
    Tec2s,
    Tec3s,
    Tec4s,
    Tec5s,
    Tec6s,
    Tec7s,
    Tec8s,
    Tec9s,
    Tec10s,
    Tec11s,
    Tec12s,
    Tec13s,
    Tec14s,
    Tec15s,
    Tar1s,
    Tar2s,
    Tar3s,
    Tar4s,
    Tar5s,
    Tar6s,
    Tar7s,
    Tar8s,
    Tar9s,
    Tar10s,
    Tar11s,
    Tar12s,
    Tar13s,
    Tar14s,
    Tar15s,
    Tar16s,
    Tar17s,
    Tar18s,
    Tar19s,
    Tar20s,
    Tar21s,
    Tar22s,
    Tar23s,
    Tar24s,
    Tar25s,
    Tar26s,
    Tar27s,
};
